# 1회전 수행 기록

> 대상 빌드 v1.0 · 계획서: [test-plan.md](test-plan.md) · 순서: [test-run-checklist.md](test-run-checklist.md)
>
> 세션별로 채운다. 결과 열의 뜻은 [테스트 케이스](test-cases.md) 의 표기와 같다.

## 환경

```
빌드      v1.0 (2026-09-03 제출본) · Windows x64
OS        Windows 11 Home 24H2 (22631)
모니터     2560 × 1440 · 게임은 창 모드 1920 × 1080
수행자     손주환
```

---

## 세션 1 — 시작 화면과 설정 · 2026-09-23

| TC | 결과 | 비고 |
|---|---|---|
| TC-UI-01 첫 프레임 글자 깜빡임 | ✔ | |
| TC-UI-02 시작 화면 버튼 | ✔ | 게임 시작 · 도움말 · 설정 · 종료 |
| TC-UI-04 설정 항목 | ✔ | 해상도 · 화면 모드 · 전체 · 배경음 · 효과음 + 언어 |
| TC-SND-02 BGM | ✔ | |
| TC-SND-03 볼륨 즉시 반영 | ✔ | △ 였던 항목 |
| TC-UI-07 영어 안내 문단 넘침 | ✔ | 최종 빌드에서 수정된 것이 유지됨 |
| TC-EDGE-27 1280×720 UI | ✔ | |
| TC-UI-05 설정 저장 유지 | ✔ | △ 였던 항목 |
| **TC-UI-11 안내와 실제 조작의 일치** | **✘** | 아래 발견 1 |

스크린샷 — `start.png` · `controls.png` · `settings.png` · `controls_en.png` · `settings_en.png`

### 발견 1 — 조작 안내에 보상 선택 키가 없다

도움말 화면의 조작표는 다섯 줄이다.

```
← →        이동
Space      점프
F          무기 사용
A          무기 교체 · 대시
ESC        설정
```

오른쪽 설명에 *"보상방에서 셋 중 하나를 고른다"* 가 있지만 **어떤 키인지가 없다.** 실제로는
클릭과 숫자키 `1` · `2` · `3` 이 둘 다 동작한다 (`StageRunner.Update`). 영어 도움말도 같다.

**재시작 `R` 도 조작표에 없다.** 다만 결과 화면에 재시작 버튼이 있어 진행이 막히지는 않는다.

이슈로 등록했다 — 아래 결함 리포트.

### 발견 2 — 효과음 볼륨이 0 으로 시작했다 (결함 아님)

켰더니 효과음이 0 이었다. 레지스트리의 저장값을 확인했다.

```
master   1.0      코드 기본값 1.0
bgm      1.0      코드 기본값 0.8    ← 저장된 값이 기본값과 다르다
sfx      0.0      코드 기본값 1.0    ← 저장된 값
```

**빌드 결함이 아니라 이전 실행에서 저장된 값이다.** 다만 이것이 드러낸 문제가 따로 있다 —
**이 환경은 "처음 설치한 사람"의 상태가 아니다.** `TC-UI-04` · `TC-UI-05` 는 기존 설정이
남은 상태에서만 확인된 것이고, 첫 실행 경로는 한 번도 안 봤다.

`TC-UI-10`(첫 실행 기본값)을 새로 세웠다. 수행하려면 `HKCU\Software\WeaponHero\WeaponHero`
를 내보내 백업한 뒤 지우고 실행한다.

**확인했다 — 통과.** 레지스트리를 비우고 실행하니 효과음 1 · 배경음 0.8 로 떴다. 코드의
기본값(`GameSettings.Load` 의 `GetFloat` 두 번째 인자)과 일치한다. 즉 처음 켜는 사람은
정상적인 소리로 시작하고, **세션 1 에서 본 효과음 0 은 이 환경만의 상태였다.**

같은 형태의 함정이 남아 있는지 생각해 둘 것 — 해상도 · 화면 모드 · 언어도 저장되는
값이라, 기존 설정이 있는 환경과 없는 환경에서 결과가 다를 수 있다.

---

## 세션 2 — 전투 기본 · 2026-09-23

26항목. **25 통과 · 1 이연.** 새 결함 없음.

| 묶음 | TC | 결과 |
|---|---|---|
| 조작 | MOVE-01 · 02 · 03 · 04 · UI-08 | ✔ 전부 |
| 공격 | CMB-01 · 02 · 06 · 03 · SND-01 | ✔ 전부 |
| 스왑 | SWAP-01 · 05 · 04 · 03 · 02 · **06** · EDGE-05 | ✔ 전부 |
| 스왑 경계 | EDGE-06(공격 중) · EDGE-07(공중) | ✔ |
| 공중 공격 착지 | **CMB-08** | ⏸ 재현됨 |
| 피격 | CMB-07 · 04 · 05 | ✔ 전부 |
| 지형 | MOVE-05 · EDGE-16 · EDGE-08 · ROOM-06 | ✔ 전부 |

스크린샷 — `combat.png` · `swap.png`

### TC-SWAP-06 — 미확인이던 주장이 검증됐다

대시 중 이동 · 점프 · 공격 · 재스왑이 **전부 막히는 것**을 확인했다. 포트폴리오 8장의
권한표가 *"스왑은 다섯을 전부 끈다"* 를 주장하는데, 지금까지 **코드 선언만 확인했고
눌러본 기록이 없었다.** 이제 근거가 둘이 됐다.

### TC-CMB-08 — 알려진 미해결이 그대로 재현

공중 공격 → 착지 → **이동하며 공격이 이어진다.** 최종 빌드 노트에 적힌 그대로다
(`AttackState.startedGrounded` 가 진입 시점의 값을 계속 들고 있다).

**새 결함으로 올리지 않는다.** 이미 기록돼 있고 고치지 않은 근거도 남아 있다 —
`!IsGrounded` 로 바꾸면 지상 공격 중 몸이 뜨는 경우의 동작까지 같이 정하게 되는데
그 재료가 1차에 없다. 이번 수행은 **그 판단이 여전히 유효한지 확인한 것**이다.

### 피격 항목이 처음으로 제대로 확인됐다

`TC-CMB-07`(적 공격이 체력을 깎는다)은 W3 에서 `HurtBox.health` 미배선으로 △ 였다.
W4 에서 배선됐지만 그 뒤로 이 항목만 따로 확인한 기록이 없었다. 이번에 확인했다.

## 세션 3 — 적과 방 진행 · 2026-09-23

19항목. **17 통과 · 2 실패.** 결함 둘 다 신규다.

| 묶음 | TC | 결과 |
|---|---|---|
| 적 | ENM-01 · 03 · 04 · 05 · 06 · 07 | ✔ |
| 보스 | **ENM-09**(비중단) | ✔ — △ 였던 항목 |
| 보스 | **ENM-11**(예고 색) | ✘ [#150](../../issues/150) |
| 무기 | WPN-04 · 01 · 02 · 03 | ✔ |
| 방 | ROOM-01 · 02 · 03 · EDGE-18 · EDGE-19 | ✔ |
| 보상 | **RUN-08**(진입 표시) | ✔ — W4 실패분 회귀 통과 |
| 보상 | **RUN-10**(선택 후 패널 닫힘) | ✘ [#149](../../issues/149) |
| 기타 | RUN-09(처치 회복) | ✔ |

스크린샷 — `portal.png`

### 보스가 안 끊기는 것은 의도다

`TC-ENM-03`(피격이 적 행동을 끊는다)에서 **보스만 안 끊겼다.** 이것은 결함이 아니라
설계다 — 보스 패턴 셋이 전부 `interruptible: 0` 이다. 잡몹 기준으로 `TC-ENM-03` 은
통과이고, 동시에 **△ 로 남아 있던 `TC-ENM-09`(보스 패턴 비중단)가 채워졌다.**

기대 결과를 "적"으로 뭉뚱그려 쓴 탓에 수행 중에 판정이 헷갈렸다. **잡몹과 보스가 다른
규칙을 따르는 항목은 TC 를 나눠야 한다.**

### 두 결함은 같은 뿌리에서 나왔다

`#149`(숫자키로 고르면 패널이 안 닫힌다)를 파고드니 `StageRunner` 에 주석이 있었다.

> `#103 이 버튼을 붙이면 Update 의 폴링만 사라지고 이 메서드는 남는다`

**숫자키 폴링은 원래 지울 코드였다.** #103 에서 버튼을 붙이며 지웠어야 했는데 남았고,
그래서 세션 1 의 `#148`(조작 안내에 보상 선택 키가 없다)이 생겼다. 안내를 빠뜨린 게
아니라 **안내할 생각이 없던 조작이 코드에 남아 있었다.**

두 이슈를 따로 열었지만 고칠 때는 한 번에 정해야 한다 — 숫자키를 지울지, 정식 기능으로
올릴지.

## 세션 4 — 보상 경계 · 2026-09-23

10항목. **9 통과 · 1 실패.** 실패는 캡처를 보다 발견한 것이다.

| TC | 결과 |
|---|---|
| RUN-06(3택) · RUN-07(가진 무기 제외) · WPN-05(강화 체감) | ✔ |
| EDGE-13(선택 전 출구) · EDGE-14(재선택 불가) | ✔ |
| EDGE-11(보상방 스왑) · EDGE-09(무료 스왑) · EDGE-12(숫자키) | ✔ |
| EDGE-15(강화 승계) | ✔ |
| **EDGE-10**(풀피면 2택) | ✔ — 에디터 없이 확인됐다 |
| **UI-12**(카드 정렬) | ✘ [#151](../../issues/151) |

스크린샷 — `reward.png`

### 에디터로 만들려던 상태가 플레이 중에 나왔다

`TC-EDGE-10`(체력이 가득한 채 보상방 진입 → 2택)은 상태를 만들기 어려워 **에디터 항목으로
미뤄둔 것**이었다. 그런데 캡처한 화면에 카드가 둘뿐이고 체력 구슬이 가득 차 있었다.

`Compose()` 는 `HP < Max` 일 때만 회복 카드를 넣으므로, **회복 카드가 없다는 것 자체가
체력이 가득했다는 증거다.** 빌드에서 확인한 것으로 기록한다.

### 실패는 결과 표가 아니라 캡처에서 나왔다

`TC-UI-12`(카드 정렬)는 수행 목록에 없던 항목이다. 스크린샷을 보다가 **카드 둘이 왼쪽에
쏠리고 오른쪽 3분의 1이 빈 것**을 발견했다. 플레이 중에는 눈에 안 들어왔다.

**캡처를 남기는 것이 그 자체로 검증 수단이다.** 화면을 정지시켜 놓고 보면 플레이하면서
지나친 것이 보인다.

### #149 의 중요도가 확정됐다

`TC-EDGE-14`(재선택 불가)가 통과해서, 패널이 남아 있어도 **보상이 두 번 적용되지는
않는다**는 것이 확인됐다 (`resolved` 플래그). [#149](../../issues/149) 는 Major 유지다.

## 세션 5 — 보스와 런 종료 · 2026-09-23

9항목. **7 통과 · 1 실패 · 1 해당 없음.**

| TC | 결과 |
|---|---|
| ENM-08(보스 3패턴) · RUN-01(10칸 완주) · RUN-02(클리어) | ✔ |
| UI-03(ESC 정지) · EDGE-21(정지 중 조작 차단) · UI-06(전투 씬 언어 항목 없음) | ✔ |
| EDGE-23(정지 중 해상도 변경 — HUD) · EDGE-25(Alt+Tab 복귀) | ✔ |
| **EDGE-28**(해상도 변경 후 설정창 위치) | ✘ [#152](../../issues/152) |
| **EDGE-22**(정지 중 언어 변경) | N/A — 아래 |

스크린샷 — `boss.png` · `pause.png`

### TC 가 없는 조작을 묻고 있었다

`TC-EDGE-22`(일시정지 중 언어를 바꾸면 즉시 반영되는가)는 **애초에 할 수 없는 조작이다.**
전투 씬 설정에는 언어 항목이 없다 (커밋 `5578ebc`). 바로 옆의 `TC-UI-06` 이 *"전투 씬
설정에는 언어가 안 보인다"* 를 확인하는 항목인데, 두 TC 가 서로 모순된 상태로 목록에
있었다.

**경계 TC 를 코드에서 뽑을 때 생긴 구멍이다.** 설정 화면에 언어 항목이 있는 것만 보고
"정지 중에도 바꿀 수 있다"고 가정했는데, 씬에 따라 항목이 달랐다. 결과를 `N/A`(해당
없음)로 두고 표기에 그 뜻을 추가했다 — **실패도 미확인도 아니고, 물음 자체가 성립하지
않는 경우다.**

### HUD 는 멀쩡한데 설정창만 어긋난다

`TC-EDGE-23` 은 HUD 기준으로 통과다. 그런데 같은 조작에서 **설정창이 위로 치우치고
오른쪽이 화면 밖으로 나간다.** 항목 하나가 두 대상을 보고 있어서 결과가 갈렸고,
설정창 쪽을 `TC-EDGE-28` 로 따로 세웠다.

Canvas Scaler 가 `Match = 1`(세로만 맞춤)이고 설정 프리팹 내부가 중앙 앵커 + 고정
오프셋이다. HUD 가 안 깨지는 것으로 보아 **앵커 구성의 차이**로 보이지만, 시작 화면에서도
같은 증상인지 확인해야 원인이 반으로 줄어든다 (열린 창의 재계산 문제인지, 스케일러
문제인지).

## 세션 6 — 사망과 재시작 · 2026-09-23

9항목 **전부 통과.** 그런데 **결함 하나가 캡처에서 나왔다** — 이번 회전에서 두 번째다.

| TC | 결과 |
|---|---|
| EDGE-01(체력 0 고정) · EDGE-04(사망 순간 공격) | ✔ |
| RUN-03(사망 → 결과) · EDGE-02(사망 후 추가 피격) | ✔ |
| UI-09(영어 결과 화면) · RUN-04(R 재시작) · EDGE-20(R 연타) | ✔ |
| RUN-05(재시작 초기화) · EDGE-17(포탈 위 사망) | ✔ |
| **UI-13**(결과 화면 텍스트 배치) | ✘ [#153](../../issues/153) |

스크린샷 — `result.png` · `result_en.png`

### 영어에서 요약이 안내와 겹친다

`Sword +0 · Spear +0` 의 `+0` 이 다음 줄 `Press R or the` 위에 겹쳐 **둘 다 못 읽는다.**
`TC-UI-09`(결과 화면이 영어로 나온다)는 통과다 — **번역은 되어 있고 배치만 깨진다.**

최종 빌드에서 `TC-UI-07`(영어 안내 문단이 상자를 넘침)을 Auto Size 로 고쳤는데, **그때
고친 것은 시작 화면 하나였다.** 같은 축(영어가 1.4~1.9배 길다)의 다른 화면을 훑지
않았고, 그것이 여기서 나왔다.

같은 화면에서 넷이 더 보였다 — 영어 버튼 라벨 `Main Menu` 가 두 줄로 쪼개짐,
`for play again` 문법, 한국어 줄바꿈이 단어 중간에서 끊김(`할려/면`), `할려면` 맞춤법.
증상은 다섯이지만 화면은 하나라 이슈 하나에 모았다.

### 통과한 항목 중에 확인된 것

결과 화면 안내에 **`R 키 또는 재시작 버튼`** 이 적혀 있다. 세션 1 의
[#148](../../issues/148) 에서 *"재시작 R 이 조작표에 없다"* 를 부수로 지적했는데,
**결과 화면이 그것을 알려주고 있다.** 실제 문제는 보상 선택 키 하나로 좁혀진다.

### 두 번 다 캡처에서 나왔다

세션 4 의 `TC-UI-12`(보상 카드 정렬)도, 이번 `TC-UI-13` 도 **수행 목록에 없던 항목**이고
스크린샷을 보다가 발견했다. 플레이 중에는 눈이 다음 조작으로 가 있어서 화면 구석을
안 본다. **정지된 화면을 보는 것이 따로 필요하다.**

## 세션 7 — 시드와 방 순서 · 2026-09-24 · 에디터

**전부 통과.** 이 세션만 환경이 에디터다.

| 항목 | 결과 |
|---|---|
| 자동 테스트 — EditMode 21개 | ✔ 전부 통과 · **개수도 21 로 확인** |
| TC-ROOM-04 · 05 — 시드 고정 후 2회 실행 | ✔ 같은 순서 · 전투방 중복 없음 |
| TC-EDGE-03 — 풀피에서 처치 회복 | ✔ 30 을 안 넘는다 |
| TC-BLD-05 — 테스트 값 되돌리기 | ✔ `seed -1` · `roomCount 5` · `maxHealth 30` |

### 개수를 세는 것이 절차다

테스트가 21개로 보이는지 눈으로 확인했다. `[Test]` 가 빠지면 **조용히 안 돌고 초록이
뜬다** — 세션이 아니라 자동화 작업(#141)에서 실제로 겪은 일이다. 통과 여부와 별개로
**몇 개가 돌았는가**를 매번 본다.

### 되돌리기를 수행 절차에 넣어둔 것이 맞았다

에디터에서 `seed` 를 고정했고, 체크리스트 마지막 줄대로 `-1` 로 되돌린 뒤 저장했다.
`git status` 에 씬이 수정됨으로 떴지만 **줄바꿈 차이뿐이고 값은 그대로**였다.

`roomCount` · `seed` · `maxHealth` · `healAmount` 는 빌드 전 확인 목록(TC-BLD-05)에
있는 값들이고, 되돌리지 않으면 **다음 빌드가 테스트 값으로 나간다.**

## 세션 8 — 장시간 구동 · 2026-09-24

30분 방치. **2 통과 · 1 실패(신규).**

| TC | 결과 |
|---|---|
| TC-EDGE-26 — 30분 구동 | ✔ 조작 정상 · 프레임 유지 |
| TC-BLD-09 — `Player.log` 에 예외 | ✔ 0건 |
| **TC-BLD-10** — 오탐 경고 | ✘ [#154](../../issues/154) |

### 로그 판정

| 줄 | 판정 |
|---|---|
| `Settings: 드롭다운이 비어 있다` | **오탐** — 전투 씬은 언어 항목을 일부러 뺀 구성이다 |
| `d3d12: failed to query info queue interface` | 무해 — 드라이버 디버그 레이어가 없을 때의 정보성 메시지 |
| `GarbageCollector disposing of ComputeBuffer` | 무해 — 종료 시 URP 내부 정리. 프로젝트 코드에 `ComputeBuffer` 가 없다 |
| 예외 · NullReference | **0건** |

### 경고가 닳는다

`GameSettings.Verify()` 가 드롭다운 셋을 한 조건으로 묶어 검사해서, 전투 씬에 들어갈
때마다 경고가 남는다. **동작에는 영향이 없지만 매 실행마다 뜨는 경고가 하나 있으면
진짜 배선 누락이 생겨도 같은 줄에 섞인다.** W3 에서 `HurtBox.health` 미배선을 놓친 것이
그 종류였다.

`TC-BLD-10`(로그에 오탐 경고가 없다)을 새로 세웠다. **로그를 검증 수단으로 쓰기로 한
이상, 로그가 깨끗한 것도 확인 대상이다.**

### 다섯 빌드 만에 지운 줄

장시간 구동은 W1~최종까지 **다섯 번 연속 "확인 안 한 것"** 에 있었다. 이번에 처리해서
그 목록에서 빠진다. 남은 셋(게임패드 · 다른 PC · 다른 모니터)은 이번 회전에서 **범위
밖으로 명시한 것**이라 성격이 다르다.

---

## 1회전 종료 — 요약

| | |
|---|---|
| 기간 | 2026-09-23 ~ 09-24 · 세션 8회 |
| TC | **110항목** |
| 통과 | 80 |
| 자동 판정 | 9 |
| 실패 | 11 (과거 빌드 수정분 4 + **이번 회전 신규 7**) |
| 이연 | 1 (`TC-CMB-08` — 근거가 기록에 있는 미해결) |
| 해당 없음 | 1 (`TC-EDGE-22` — 존재하지 않는 조작) |
| **확인 못 함** | **0** |
| 신규 결함 | [#148](../../issues/148) · [#149](../../issues/149) · [#150](../../issues/150) · [#151](../../issues/151) · [#152](../../issues/152) · [#153](../../issues/153) · [#154](../../issues/154) |

### 종료 기준 확인

- [x] P0 항목을 전부 수행했다
- [x] 수행하지 못한 항목이 없다 (△ 0)
- [x] Fail 은 전부 이슈로 등록하고 TC 에 번호를 연결했다
- [x] 재현율을 확인했다
- [ ] 결과 보고서 — 다음 단계

### 이번 회전에서 배운 것

**실패 일곱 중 셋이 캡처에서 나왔다.** `TC-UI-12`(보상 카드 정렬) · `TC-UI-13`(결과 화면
겹침) · `TC-BLD-10`(오탐 경고)은 수행 목록에 없던 항목이다. 플레이 중에는 눈이 다음
조작에 가 있어 화면 구석과 로그를 안 본다. **정지된 화면과 로그를 따로 보는 절차가
필요하다.**

**TC 자체의 결함도 셋 나왔다.** 존재하지 않는 조작을 묻는 항목(`TC-EDGE-22`), 한
항목이 두 대상을 보는 것(`TC-EDGE-23` — HUD 와 설정창), 잡몹과 보스가 다른 규칙인데
하나로 묶인 것(`TC-ENM-03`). **수행은 제품만 검사하는 게 아니라 TC 도 검사한다.**

**같은 축을 한 번 고치고 다른 화면을 안 훑은 것이 드러났다.** 영어 문자열 길이 문제를
최종 빌드에서 시작 화면 하나만 고쳤고, 결과 화면에서 같은 것이 나왔다(#153).

---

## 결함 리포트

### BUG-01 · 조작 안내에 보상 선택 키가 없다 — [#148](../../issues/148)

```
발견 TC     TC-UI-11
빌드        v1.0 (2026-09-03)
환경        Windows 11 · 창 모드 1920×1080 · 빌드
재현율      3/3

재현 절차
  1. 게임을 실행한다
  2. 시작 화면에서 [도움말] 을 누른다
  3. 조작표를 확인한다

기대 결과   보상 선택에 쓰는 키(숫자키 1·2·3 또는 클릭)가 안내에 있다
실제 결과   "보상방에서 셋 중 하나를 고른다" 라고만 적혀 있고 키가 없다.
            재시작 R 도 조작표에 없다
            한국어 · 영어 안내 모두 동일

중요도      Minor      진행은 막히지 않는다. 클릭이 자연스럽게 눈에 띈다
우선순위    Normal     첫 화면의 안내이고 문자열 수정으로 끝난다
첨부        controls.png · controls_en.png

원인 추정   기능(숫자키)이 나중에 들어갔거나, 안내 문구가 "클릭"만 가정하고 쓰였다.
            README 조작법에서도 같은 문장이 한 번 지워졌다가 되살아난 이력이 있다
```
