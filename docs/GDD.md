# 게임 기획서 (GDD)

> 상태: **초안 / 기획 틀** · 최종 수정 2026-07-28
> 확정되지 않은 항목은 `TBD` 로 표기. 결정되는 대로 이 문서를 갱신한다.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 한 줄 컨셉 | 두 자루의 무기를 실시간으로 바꿔 끼우며 싸우는 2D 액션 로그라이크 |
| 장르 | 2D 횡스크롤 액션 플랫포머 + 로그라이크 |
| 플랫폼 | PC (Windows), 키보드 + 게임패드 |
| 엔진 | Unity 6000.3.15f1 / URP 17.3 (2D Renderer) |
| 목표 | **포트폴리오** — 콘텐츠 물량보다 핵심 루프의 완성도와 손맛 |
| 개발 규모 | 1인 · **6주 · 주 20시간 = 총 약 120시간** |
| 우선순위 | **완성된 루프 > 전투 깊이 > 콘텐츠 물량** |
| 주 레퍼런스 | 스컬: 히어로 슬레이어 (Skul: The Hero Slayer) |

---

## 2. 레퍼런스 분석 — 스컬에서 무엇을 가져오는가

### 가져올 것

- **교체가 곧 조작 리듬** — 스왑이 단순 메뉴 조작이 아니라 전투 중 계속 눌러야 하는 액션.
  스왑 자체에 이동/회피 성능을 부여해 "교체를 아끼면 손해"인 구조를 만든다.
- **짧고 밀도 높은 전투** — 방 하나당 10~30초. 적은 수가 아니라 밀도로 압박.
- **강한 히트 피드백** — 히트스탑, 화면 흔들림, 넉백, 이펙트. 스컬 손맛의 8할.
- **런마다 달라지는 빌드** — 무기 조합 + 유물 시너지로 매 판 다른 플레이.

### 버릴 것 / 바꿀 것

- **머리 교체 → 무기 교체** (아래 3장 참조)
- **방대한 캐릭터 수** — 스컬은 캐릭터 수십 종이 셀링포인트. 포트폴리오 규모에서는 불가능하고,
  소수를 깊게 파는 편이 오히려 평가에 유리하다.
- **복잡한 메타 성장 트리** — 최소한으로 축소 (7장 참조)

---

## 3. 핵심 시스템 — 무기 교체

> **작업 가정:** 스컬의 "2슬롯 실시간 교체" 구조는 유지하되, 교체 대상을 **머리 → 무기**로 바꾼다.
> 재검토 가능하나, 아래 근거로 현재는 무기안을 기준으로 설계를 진행한다.

### 왜 무기인가 (포트폴리오 관점)

머리 교체는 캐릭터 1종당 **전신 스프라이트 + 애니메이션 풀세트**가 필요하다. 1인 개발에서 이건
콘텐츠 1종 추가 비용이 가장 비싼 구조다. 무기 교체는 **공용 캐릭터 리그 하나**를 쓰고 무기별
공격 모션만 추가하면 되므로, 같은 기간에 3~4배 많은 빌드 다양성을 만들 수 있다.
"시스템 설계 능력"을 보여주는 게 목적이라면 물량보다 조합의 깊이가 낫다.

### 규칙

- 무기 슬롯 **2개**. 언제든 `스왑` 키로 즉시 교체.
- **스왑에 이동 성능 부여** — 스왑 순간 짧은 전진 대시 + 무적 프레임(i-frame).
  즉 스왑은 회피기이기도 하다. 쿨타임 `TBD`(스컬 기준 3~4초 감각).
- **대기 중인 무기는 쿨타임이 회복된다** — 스킬을 다 쓴 무기를 내려두고 다른 무기로 싸우다
  다시 돌아오는 순환이 기본 리듬.
- 무기마다: **평타 콤보 1종 + 고유 스킬 2종**.

> **1차에는 평타만 들어간다.** 스킬과 대기 쿨타임 회복은 W2 에서 이연했다(8장 · 로드맵).
> 그래서 위의 "기본 리듬"은 1차에서 성립하지 않고, 스왑은 **회피기 + 평타 성격 전환**으로만
> 작동한다. 무기를 번갈아 쓸 이유는 리치·타이밍 차이(#62)와 스왑 자체의 대시·i-frame 에서
> 나온다.
>
> 이 문단을 지우지 않는 이유는 **이연이 삭제가 아니기 때문**이다. 후속에서 여기로 돌아온다.
- 무기 등급 `TBD` (일반/희귀/유니크 등 — 등급제를 둘지 자체가 미결정)

### 무기 아키타입

1차 3종. **에셋 파츠에 맞춰 확정했다** ([10.1](#101-확정--pixem-파츠-조합형-픽셀-캐릭터)).

| 아키타입 | 클립 | 성격 | 스왑 시너지 예시 |
|---|---|---|---|
| **검** (기본) | `AttackSlash` | 균형, 넓은 범위 | 무엇과 짝지어도 무난한 기준점 |
| 창 | `AttackPrick` | 중거리 리치, 찌르기 | 검보다 먼 거리에서 견제 |
| 활 | `AttackShoot` | 원거리 견제 | 근접 무기와 짝지어 거리 관리 |

**캐릭터는 6개를 미리 구웠다** — 검 · 단검 · 창 · 지팡이 · 활 + 무기 없는 **기본형**.
1차 구현은 위 3종만 한다. 로드맵 W2 가 "무기 3종 8h"인데 무기 하나당 평타 + 스킬 2종을
설계해야 해서 그 이상은 초과한다.

기본형(`Character_Base`)은 게임에 쓰지 않는다. **새 무기 캐릭터를 구울 때 시작점**으로
남겨둔 것이다. 파츠를 손으로 다시 고르면 외형이 미묘하게 어긋나므로 이 기준에서
무기 필드만 바꿔 굽는다.

미리 구워둔 덕에 **후속 추가 비용이 갈린다.**

| 후속 후보 | 남은 작업 |
|---|---|
| 단검 | 캐릭터 있음 · 클립 `AttackPrick` · 발사체 없음 → **SO 하나** |
| 지팡이 | 캐릭터 있음 → SO + 마법 발사체 (활과 다르게 동작하면 별도 작업) |
| 대검 | 굽기 + SO. 클립·컨트롤러가 검과 같아 수치만 다르게 넣으면 된다 |
| 도끼 등 | 파츠에는 있으나 아직 굽지 않았다 |

**단검이 가장 싸다.** W2 에 여유가 생기면 첫 후보다.

#### 아키타입은 스프라이트가 아니라 타이밍이 만든다

무기 파츠는 20종이 넘는데 **공격 클립은 4종뿐**이다. 여러 무기가 같은 클립을 공유할
수밖에 없다 — 검·대검·도끼가 전부 `AttackSlash` 다. 그래서 **무게감은 그림이 아니라
타이밍에서 온다.**

| 요소 | 무겁게 | 가볍게 |
|---|---|---|
| 클립 재생 속도 | 느리게 | 빠르게 |
| 선딜 / 후딜 | 길게 | 짧게 |
| 히트박스 크기 | 넓게 | 좁게 |
| **히트스탑** | 길게 | 짧게 |
| 넉백 | 강하게 | 약하게 |

히트스탑이 0.15초인 무기와 0.04초인 무기는 같은 스프라이트라도 손맛이 완전히 다르다.
[#12](../../issues/12) 에서 히트스탑을 이미 만들어뒀으므로 무기 SO 에 이 값들을 넣으면
9장 확장 지점 1이 그대로 동작한다 — 무기 추가가 데이터 작성이 된다.

**활은 유일하게 슬롯이 다르다.** `RightHandWeapon` 이라 손 위치까지 바뀌어서 근접/원거리
구분이 그림으로 드러난다. 조립할 때 왼손을 `Empty` 로 비워야 검과 활을 동시에 들지 않는다.

**이펙트는 `SlashEffect.png` 하나뿐이다.** 무기별 이펙트가 필요하면 W2 에서 별도로
확보해야 한다. 없어도 위 타이밍 차이로 구분은 된다.

#### 후속 — 같은 계열 안에서 무기를 늘린다

아키타입은 3~4개로 두고, **같은 계열 안에서 수치·상태이상·부가효과를 달리한 무기**를
추가한다. 검 계열에 검이 세 자루 있어도 하나는 화상, 하나는 고데미지·저속, 하나는
연타 특화면 각각 다른 선택이 된다.

목적은 **플레이 스타일 선택 유도**다. 같은 계열이라 조작은 익숙한데 수치와 효과가
달라서, 플레이어가 "내가 어떻게 싸우고 싶은지"로 고르게 된다. 아키타입만 늘리면
조작을 새로 익혀야 해서 진입 장벽이 되고, 로그라이크의 반복 플레이와 맞지 않는다.

**1차에서 지킬 것 3개.** 이것만 지키면 후속이 데이터 작성 + 효과 클래스 추가로 끝난다.

| 지킬 것 | 안 지키면 |
|---|---|
| 공격력·선딜·히트스탑·넉백을 **무기 SO 필드로** 둔다 | 값이 코드에 박혀서 무기를 늘릴 때마다 분기가 생긴다 |
| `DamageInfo` 에 **상태이상 정보를 실을 자리**를 남긴다 | 화상·빙결을 넣을 때 피격 경로 전체를 고쳐야 한다 |
| 타격 시점에 **이벤트 훅**을 둔다 | 부가효과를 무기별 `if` 로 처리하게 된다. 9장 확장 지점 4(유물)와 같은 훅을 쓰면 코드가 재사용된다 |

**아키타입 SO 와 무기 SO 를 지금 분리하지 않는다.** 1차에서는 아키타입과 무기가 1:1
이라 분리해도 이득이 없다. 계열 안에 무기가 둘 이상 생겨서 컨트롤러 참조가 실제로
중복될 때 나누면 된다 — 추상화는 중복이 보인 다음에 넣는다.

> 최종 무기 수는 8장 콘텐츠 물량 참조.

### 미결정

- 같은 무기 2개를 들 수 있는가? (중첩 강화 vs 금지)
- 스왑에 **공격 판정**을 넣을 것인가? (넣으면 공격적, 안 넣으면 순수 회피기)
- 무기 강화/합성 시스템 존재 여부

---

## 4. 코어 게임플레이 루프

```
런 시작 (기본 무기 1종)
  └→ 방 진입 → 전투 (10~30초) → 클리어
        └→ 보상 선택 (무기 / 유물 / 재화 중 택1)
             └→ 다음 방 …  (스테이지당 8~12방)
                  └→ 보스전
                       └→ 클리어 → 다음 스테이지 / 사망 → 메타 재화 정산 → 런 시작
```

**1런 목표 플레이타임:** 20~30분 (풀 클리어 기준)

---

## 5. 전투 시스템

### 조작

| 입력 | 동작 |
|---|---|
| 좌/우 | 이동 |
| 점프 | 점프 (1단 — 2단 점프는 넣지 않는다, 12.1) |
| 공격 | 평타 콤보 |
| 스킬 1 / 2 | 현재 무기 고유 스킬 |
| 스왑 | 무기 교체 + 대시 + i-frame |
| 상호작용 | 아이템 획득, 문 진입 |

Unity **Input System** 사용 (`Assets/InputSystem_Actions.inputactions` 기존 에셋 활용).

### 액션 감각 (가장 먼저 프로토타이핑할 영역)

- **코요테 타임 / 점프 버퍼** — 플랫포머 조작감의 기본. 초기부터 넣는다.
- **히트스탑** — 타격 시 수 프레임 정지. 무기 무게감을 만드는 핵심.
- **넉백 / 경직** — 적 반응. 슈퍼아머 적은 경직 무시.
- **선입력(input buffer)** — 콤보 중 다음 입력 선접수.
- **피격 무적 + 무적 시각화**

> 위 항목들은 "나중에 추가"가 가장 안 되는 것들이다. 프로토타입 단계에서 확보한다.

### 적 설계 원칙

- 적은 **역할**로 분류: 근접 돌진형 / 원거리 견제형 / 방패(정면 방어) / 광역 압박형
- 각 적은 **공격 전 예고 모션**을 반드시 가진다 (패턴 학습 가능해야 함)
- 방 구성은 단일 종류가 아니라 **역할 조합**으로 압박을 만든다

---

## 6. 로그라이크 구조

### 스테이지 / 방

- 스테이지 = 방 8~12개 + 보스방 1개
- 방은 **수제작 프리팹 풀에서 랜덤 추출** (완전 절차적 생성 아님)
  → 1인 개발에서 절차 생성은 품질 관리 비용이 너무 크다. 수제작 방 + 랜덤 배치가 현실적.
- 방 타입: 전투 / 보상 / 상점 / 휴식 / 이벤트(`TBD`) / 보스

### 보상 설계

방 클리어 시 **선택지 3개 중 1개** — 선택이 곧 빌드 방향 결정.
"강해지는 느낌"보다 "무엇을 포기했는가"가 기억에 남게 설계한다.

### 유물 / 시너지

- 유물 = 패시브 효과. 런 종료까지 유지.
- **무기 아키타입과 연동되는 유물**을 일부 배치해 빌드 방향성을 만든다
  (예: "근접 무기 피격 시 반격", "스왑 직후 3초간 공격속도 +30%")
- 스왑 시스템과 엮인 유물이 이 게임의 정체성 — 최소 3~4종은 확보

---

## 7. 메타 성장 (사망 후)

> **1차 범위(6주) 밖 — 후속 단계로 이연.**

메타 성장은 여러 런을 반복해야 체감되는 시스템이라, 런 하나가 겨우 완성되는 규모에서는
투자 대비 회수가 가장 나쁘다. 1차에서는 사망 시 결과 화면만 띄우고 즉시 재시작한다.

**나중에 붙일 자리는 지금 남겨둔다:**
- 런 종료 시 `RunResult`(획득 재화, 처치 수, 도달 방, 사용 무기)를 반환하는 구조로 만든다.
  1차에서는 결과 화면 표시에만 쓰지만, 그대로 메타 성장의 입력값이 된다.
- 저장/로드는 1차에서 구현하지 않되, **세이브 대상이 될 데이터는 한 곳에 모아둔다**
  (런타임 상태와 영구 상태를 섞지 않는다). 이것만 지키면 나중 추가가 하루 작업이다.

---

## 8. 콘텐츠 물량

**1차(6주 / 120시간)는 아래 "1차" 열까지만 만든다.** 나머지는 잘라내는 게 아니라
**데이터만 추가하면 되는 상태**로 남겨둔다 (9장 확장 지점 참조).

| 항목 | **1차 (6주)** | 후속 |
|---|---|---|
| 스테이지 | 1 | 2~3 |
| 방 프리팹 | 6~8 | 20+ |
| 무기 | **3종** (검 / 창 / 활) | 8종 (단검 · 지팡이 · 대검 · 도끼 등) |
| 적 | **3종** (돌진 / 원거리 / 방패) | 8종 |
| 보스 | 1 | 2~3 |
| 유물 | 8종 | 20종+ |
| 방 타입 | 전투 / 보상 / 보스 | +상점 / 휴식 / 이벤트 |

무기 3종 · 적 3종이면 조합이 9가지라 "매 판 다르다"는 감각은 1차에서도 충분히 나온다.

### 1차 범위 밖 (이연 — 설계에는 자리를 남긴다)

| 이연 항목 | 나중에 붙이는 방법 |
|---|---|
| 메타 성장 | `RunResult` + 영구 상태 분리 (7장) |
| 런 중간 저장 (이어하기) | `RunState` 순수 데이터 + 시드 기반 생성 (11.3) |
| 키 설정 (리바인딩) | Input System `SaveBindingOverridesAsJson` — UI만 추가 (11.4) |
| 언어 추가 (3개국어 이상) | String Table 에 열 추가 — 한/영은 1차 포함 (11.2) |
| 스테이지 2~3, 보스 2~3 | 스테이지도 데이터(SO)로 정의 → 방 풀·보스만 교체 |
| 상점 / 휴식 / 이벤트방 | 방 타입을 enum이 아닌 **데이터 + 핸들러**로 → 타입 추가가 파일 추가 |
| **무기 스킬 + 대기 쿨타임 회복** | **W2 에서 이연했다 — 후속 0순위.** `SkillData` 를 무기 SO 가 **참조로** 든다(인라인 금지). 자리가 남아 있어 붙이는 것이 데이터 작성에 가깝다 ([#51](../../issues/51) · [#52](../../issues/52)) |
| 같은 계열 안의 무기 변형 (수치·상태이상·부가효과) | 무기 SO 필드 + `DamageInfo` 확장 자리 + 타격 이벤트 훅. 셋만 지키면 데이터 작성 + 효과 클래스 추가로 끝난다 (3장) |
| 무기 등급제 | 무기 SO에 `rarity` 필드만 미리 두고 1차에서는 전부 동일 등급 |
| 무기 강화·합성 | 무기 인스턴스를 SO 직접 참조가 아닌 **런타임 래퍼**로 → 수치 변형 여지 확보 |
| 자체 아트 | 스프라이트·애니메이터를 무기/적 데이터에서 참조 → 에셋 교체가 데이터 수정 |
| 업적 | **후속에 넣는 쪽으로 기운다.** 유물과 같은 이벤트 버스를 구독 (9장 확장 지점 4번) + 영구 저장은 메타 성장에 얹음 |
| 2인 온라인 코옵 (UGS) | **여지만 남긴다** — 아래 8.1. 계획이 아니라 "막지 않는다"는 뜻이다 |

### 8.1 2인 코옵 — 여지만 남긴다

UGS(Relay + Lobby) 와 Netcode for GameObjects 로 **2인 온라인 코옵**을 붙일 여지를
남겨둔다. **후속 계획이 아니다.** 1차에서 무심코 막아버리는 것만 피한다는 뜻이다.

PvP 와 달리 코옵은 이 게임의 전제를 깨지 않는다. 협력 플레이라 화면 정지·흔들림 같은
전역 연출을 **양쪽에 함께 걸어도 이상하지 않고**, 호스트 권위 방식이면 롤백 넷코드가
필요 없다. 반면 PvP 는 히트스탑·화면 흔들림이 전부 "시간이 한 명의 것"을 전제하므로
D 에픽 전체를 다시 설계해야 한다 — 이연이 아니라 다른 게임이다.

**1차에서 지킬 것은 둘뿐이고, 둘 다 지금 공짜다.**

| 지킬 것 | 안 지키면 |
|---|---|
| 플레이어를 **static·싱글턴으로 참조하지 않는다** | 플레이어가 둘이 되는 순간 "그 플레이어"가 누구인지 정해지지 않는다 |
| 방·보상·유물 생성을 **시드 기반**으로 (11.3 에서 이미 결정) | 두 클라이언트가 다른 맵을 본다. 동기화 비용이 통째로 생긴다 |

전역 연출(`HitStop` 등)이 `static` 인 것은 무방하다 — **연출은 하나여도 되지만 플레이어는
아니다.**

붙일 때 실제로 드는 비용은 따로 있고, 그건 그때 낸다: 네트워크 계층, 카메라(둘 추적),
부활 규칙, 히트스탑을 누구 기준으로 걸지.

**판단 시점은 1차 완성 후다.** 로드맵 후속 표의 1~2번(콘텐츠 물량, 스테이지 2)을 마친
시점에 여유를 보고 정한다. 그 전에 착수하면 콘텐츠가 빈 채로 넷코드만 있는 상태가 된다.

### 일정이 밀릴 때 절단 순서

**유물 수 → 방 프리팹 수 → 3번째 적** 순으로 줄인다.
**무기 교체 시스템과 보스는 마지막까지 지킨다** — 이 둘이 포트폴리오의 핵심이다.

---

## 9. 기술 설계 방향

설치된 패키지 기준 (`Packages/manifest.json`):

- **입력**: Input System 1.19 — 액션 맵 기반, 키보드/패드 동시 지원
- **2D 애니메이션**: `com.unity.2d.animation` (스켈레탈) + `com.unity.2d.aseprite` (프레임)
  → 1차에서는 **구매한 에셋이 제공하는 방식을 그대로 따른다** (10장 참조)
- **레벨**: Tilemap + Tilemap Extras, SpriteShape
- **렌더링**: URP 2D Renderer — 2D 라이트/노멀맵은 1차 범위 밖 (나중에 켜도 무방)

### 아키텍처 방향 (초안)

- 무기 = **ScriptableObject 데이터 + 런타임 behaviour** 분리.
  데이터 주도 설계여야 무기 추가가 싸진다.
- 상태 머신 기반 플레이어 컨트롤러 (Idle / Move / Jump / Attack / Swap / Hit)
- 전투 이벤트(피격, 사망, 스왑)는 이벤트 버스로 느슨하게 연결 → 유물이 여기 훅을 건다
- 유물 = 이벤트 훅 + 효과 조합. 유물마다 클래스를 파지 않는다

### 확장 지점 — 1차에서 반드시 지킬 것

기간이 짧다고 하드코딩하면 후속 작업이 재작성이 된다. 아래 네 가지만 지키면
**이연한 항목 대부분이 "데이터 추가"로 붙는다.** 구현 비용은 거의 늘지 않는다.

| 지점 | 지킬 것 | 안 지키면 |
|---|---|---|
| **무기** | SO 데이터 + 런타임 래퍼. 무기 목록은 배열/풀로 관리. **스킬은 무기 SO 가 참조로 든다** — 인라인 금지 | 무기 추가마다 코드 수정. 스킬을 인라인하면 새 거동(완드의 범위 논타게팅 등)이 올 때 무기 SO 구조를 다시 건드린다 |
| **적 / 보스** | 판단·실행 분리 (아래 9.1). 보스도 일반 적과 같은 틀 | 보스 2 추가 시 통째로 재작성 |
| **방** | 방 = 프리팹 + 메타데이터(타입, 난이도). 타입은 **핸들러 매핑** | 상점·이벤트방 추가가 분기문 지옥 |
| **유물** | 이벤트 훅 구독 + 효과 데이터. 유물별 클래스 금지. **이벤트 이름을 유물 전용으로 짓지 않는다** (`OnRelicTrigger` ❌ / `OnEnemyKilled` ⭕) | 20종으로 늘릴 때 클래스 20개. 업적이 붙을 때 버스를 다시 판다 |

> 반대로 **1차에서 하지 말 것**: 런 중간 저장, 메타 성장 세이브, 오브젝트 풀링 최적화,
> 난이도 스케일링, **에셋 로딩 파이프라인으로서의 Addressables**.
> 나중에 붙여도 재작업이 발생하지 않는 영역이다.
> (설정 저장과 로컬라이즈는 **1차에 포함한다** — 11장 참조)

> **Addressables 주의:** Localization 패키지가 테이블 로딩에 Addressables 를 쓰기 때문에
> `Assets/AddressableAssetsData/` 가 자동 생성된다. 이건 정상이며 **그대로 둔다**
> (그룹 설정이 빠지면 클론 시 로컬라이제이션이 붙지 않으므로 커밋 대상이다).
> 1차 범위 밖인 것은 "우리가 직접 에셋을 Addressables 로 로딩하는 것"이지, 이 자동 생성분이 아니다.

> 상세 설계는 프로토타입에서 실제 문제를 만난 뒤에 확정한다. 지금 과설계하지 않는다.

---

### 9.1 적 / 보스 AI 구조

일반 적과 보스는 **같은 틀**을 쓴다. 차이는 패턴 수와 페이즈 유무뿐이다.
W3에서 일반 적으로 이 틀을 잡아두면, W5의 보스는 새 시스템이 아니라 **패턴 데이터 추가 작업**이 된다.

```
EnemyBrain                          ← 일반 적 · 보스 공용
├─ IPatternSelector   판단 층 — "다음에 무엇을 할지"   ★교체 가능 지점
├─ StateMachine       실행 층 — Idle / Chase / Telegraph / Attack / Recover / Stagger / Dead
└─ AttackPattern (SO) 내용물 — 예고 시간, 히트박스 타이밍, 이동, 후딜, 사용 조건

Boss = EnemyBrain + PhaseController (HP 임계값 → 패턴 풀 교체 + 페이즈 전환 연출)
```

**핵심 원칙: 상태머신은 "무엇을 할지" 결정하지 않는다.**
상태는 `Telegraph → Attack → Recover` 사이클을 돌리는 껍데기이고, 그 안에서 재생되는 내용물은
`AttackPattern` 데이터가 들고 있다. 패턴을 10개 추가해도 상태 수는 변하지 않는다.

**안티패턴 — 공격마다 상태를 만드는 것**
`Attack_Slam` / `Attack_Dash` / `Attack_Summon` … 이렇게 가면 패턴이 늘 때마다 상태와 전이가
같이 늘어난다. 보스 2 추가 시 상태머신을 통째로 다시 짜게 되는 전형적인 원인이다.

**패턴 실행은 코루틴 타임라인으로.**
"0.4초 예고 → 0.1초에 히트박스 On → 0.25초에 Off → 0.5초 후딜" 같은 시퀀스는
상태를 더 쪼개는 것보다 코루틴이 훨씬 읽기 쉽고 수정도 빠르다.

#### 판단 층 — 1차는 가중치 셀렉터, BT는 후속 옵션

`IPatternSelector` 를 인터페이스로 두어 **판단 층만 통째로 갈아끼울 수 있게** 한다.
실행 층(상태머신 + `AttackPattern`)은 어느 쪽을 쓰든 그대로다.

| 구현 | 시점 | 방식 |
|---|---|---|
| `WeightedPatternSelector` | **1차** | 거리·쿨타임·페이즈 조건 + 가중치 추첨 |
| `BehaviorTreeSelector` | 후속 (검토) | BT가 패턴을 고르고, 실행은 기존 층에 위임 |

**1차에 BT를 쓰지 않는 이유:** 2D 액션 보스는 "거리 보고 패턴 하나 골라 끝까지 재생"이
대부분이라 가중치 셀렉터로 충분하다. 120시간 예산에 BT 프레임워크 학습·구축 비용을 넣을 자리가 없다.

**BT로 넘어갈 가치가 생기는 신호:**
- 패턴 선택 조건이 중첩되기 시작할 때 (조건문이 3단 이상 겹침)
- 적이 **동시에 여러 판단**을 해야 할 때 — 아군과의 협조, 지형 활용, 자원 관리, 후퇴/재접근
- 보스 페이즈가 3개 이상이 되고 페이즈마다 행동 성향이 크게 달라질 때
- 패턴 수가 적 1종당 6~7개를 넘어 가중치 테이블이 관리 불가능해질 때

위 신호가 없다면 BT는 순수 오버엔지니어링이다. **인터페이스만 지금 분리해두고,
실제 도입은 신호가 관측된 뒤에 판단한다.**

> 도입하게 되면 자체 구현보다 검증된 에셋(Behavior Designer 등) 또는 Unity 공식
> `com.unity.behavior` 검토가 우선이다. 1인 개발에서 BT 프레임워크 자체 구현은 배보다 배꼽이 크다.

---

## 10. 아트 / 사운드

**1차에서는 자체 아트를 제작하지 않는다.** 120시간 예산에서 아트 제작은 들어갈 자리가 없다.
유료 에셋을 그대로 쓰고, **에셋이 픽셀이면 픽셀·스켈레탈이면 스켈레탈로 간다.**
→ 기존 미결정 항목이던 "아트 파이프라인 선택"은 이렇게 해소된다.

- 에셋 위치: `Assets/Imported/` (별도 저장소, 부모에서 gitignore 처리됨)
- **0주차에 에셋을 먼저 확정한다.** 개발 중 에셋을 찾아다니면 일정이 무너진다.
  필요 최소: 캐릭터 1 + 적 3 + 무기 이펙트 3 + 타일셋 1 + 타격음 몇 종
- 사운드: 타격음 우선. 액션 게임에서 히트 사운드는 이펙트만큼 손맛에 기여한다

교체 가능성을 위해 스프라이트·애니메이터 참조는 **무기/적 데이터(SO)에 둔다.**
나중에 자체 아트로 갈아탈 때 코드를 건드리지 않아도 된다.

### 10.1 1차 아트 — Pixem (파츠 조합형 픽셀 캐릭터)

에디터에서 파츠(몸·머리·옷·무기·방패)를 조합해 캐릭터를 굽는 도구형 에셋이다.

> **1차 한정이다.** 실제로 넣어보니 의도한 방향과 어긋나는 부분이 있어, 여유가 생기면
> 다른 에셋으로 교체한다. 다만 **W1~W6 중에는 바꾸지 않는다** — 로드맵 리스크 표의
> "개발 중 에셋 쇼핑 금지"가 그대로 적용된다. 손맛 검증에 룩은 변수가 아니다.
>
> 교체 비용은 위 원칙(참조를 데이터에 둔다)을 지키는 한 낮다. 지킬 것은 하나다 —
> **클립 이름 문자열을 코드 여러 곳에 흩뿌리지 않는다.** `"Hit"` / `"Death"` 같은
> 이름은 에셋마다 다르고, W2 에서 무기 SO 로 옮겨갈 때 함께 데이터가 된다.

| | 값 |
|---|---|
| PPU | 32 |
| 프레임 | 64 × 64 → **월드 2 유닛** |
| 필터 | Point |
| 애니메이션 | Idle / Run / Jump / Hit / Death + **공격 4종** |
| 무기 파츠 | 왼손 23종 · 활 8종 · 방패 10종 |

공격 4종이 `AttackSlash` / `AttackPrick` / `AttackMagic` / `AttackShoot` 라 3장의 무기
아키타입(검·창·활)이 그대로 대응하고 단검·지팡이가 후속 확장으로 남는다. 적 3종(돌진·원거리·
방패)도 같은 파츠로 조합할 수 있어 9장 확장 지점 2(적·보스 공용 상태머신)와 맞는다.

**단, 무기 파츠는 전부 한손 무기다.** 실루엣으로 아키타입을 구분할 수 없어서 타이밍과
판정으로 나눈다. 근거는 3장 "아키타입은 스프라이트가 아니라 타이밍이 만든다".

~~**아직 없는 것: 타일셋.**~~ **확보 완료** ([#44](../../issues/44)) — GandalfHardcore 사이드스크롤러 팩.
슬라이스 격자 32px 이라 캐릭터와 밀도가 맞는다. 벽·배경은 아직 고르지 않았다.

#### 적 에셋 — 잡몹은 Pixem, 보스는 PixelFantasy

W3 착수 전에 적 에셋을 찾아봤고 **잡몹은 Pixem 으로 굽는 원래 계획이 유효**하다는 결론이다.
파츠로 3역할(돌진·원거리·방패)을 조합할 수 있고, 무엇보다 **플레이어와 프레임 방식·픽셀 밀도가
같다.**

| 후보 | 결과 | 이유 |
|---|---|---|
| **SPUM** | ❌ 탈락 | 컷아웃 방식 |
| **PixelFantasy** (PixelMonsters) | ⭕ **W5 보스용으로 확보** | 프레임 방식 · 크기가 보스급 |

**SPUM 이 탈락한 이유가 스타일이 아니라 기법이다.** 클립을 열어보니 `classID: 4`(Transform)가
93개인데 `m_PPtrCurves` 가 비어 있었다 — **스프라이트를 한 장도 안 바꾸고** 팔·다리·머리를
트랜스폼으로 회전시킨다.

```
SPUM    Root/BodySet/P_Body/ArmSet/ArmR/P_RArm  …   m_SampleRate: 60
Pixem   m_PPtrCurves 에 스프라이트 키프레임          30fps 격자
```

그러면 셋이 어긋난다 — **픽셀이 대각선으로 뭉개지고**, 60fps 보간이라 **적만 미끄러지듯 움직이고**,
관절이 어긋난 실루엣이 나온다. `m_SampleRate` 를 낮춰도 회전은 남고 회전을 없애면 애니메이션이
사라지므로 **설정으로 좁혀지지 않는다.** 클립 15종에 `Stun`·`Skill` 까지 있는 게 아까웠지만
플레이어 옆에 못 세운다.

**PixelFantasy 는 방식이 맞다.** `m_PPtrCurves` 에 키프레임이 있고 `SpriteRenderer` 만 쓴다.
PPU 가 16 이라 처음엔 탈락으로 봤는데 **프레임이 128×128 이라 계산이 달랐다.**

```
플레이어   64px  / 2유닛 = 32 px/유닛
몬스터     128px / PPU 32 → 4유닛 = 32 px/유닛   ← 밀도 일치. 크기는 두 배
```

**PPU 를 32 로 바꾸면 밀도가 정확히 맞고 크기가 플레이어의 두 배**가 된다. 잡몹으로는 크고
보스로는 맞다 — 팩 이름이 `BossPack1` 인 그대로다.

클립이 `Idle`·`Run`·`Attack`·`Hit`·`Die`·`Fire`·`Heal`·**`Ready`** 여덟이고 컨트롤러 하나를
6종이 공유한다 (10.2 의 오버라이드 컨트롤러 구조와 같은 형태).

**`Ready` 가 예고 모션이다.** 5장이 필수로 잡은 항목인데 클립으로 존재한다 — 지금 `EnemyDummy`
가 색을 바꿔 예고하던 것을 대체한다.

> **W5 에 찾지 않고 지금 확보한 이유**는 그때 찾으면 "개발 중 에셋 쇼핑"이 되기 때문이다.
> 2.2MB 라 부담도 없다. 프리팹에 샘플 AI 스크립트가 넷 붙어 있는데 우리는 `EnemyBrain` 을
> 쓰므로 **스프라이트와 컨트롤러만** 가져온다 — 원본을 건드리면 에셋 업데이트 때 충돌한다.

**적 에셋 판단 기준은 셋이다.** 다음에 또 찾게 되면 이 순서로 거른다.

1. **프레임 방식인가** — `m_PPtrCurves` 가 비어 있으면 탈락. 조정으로 안 좁혀진다
2. **픽셀 밀도가 32 px/유닛인가** — PPU 가 아니라 `프레임 픽셀 ÷ 월드 유닛` 으로 계산한다
3. **예고 모션이 있는가** — 없어도 색·스케일로 대신할 수 있지만 있으면 크게 이득

#### 아트 제약 — 낙하 전용 포즈가 없다

시트의 스프라이트를 전부 확인한 결과 이 목록이 전부다.

```
Idle 4장 · Run 4장 · Jump 2장 · Hit 2장 · Death 3장
AttackSlash 3장 · AttackPrick 4장 · AttackShoot 4장 · AttackMagic 3장
```

`Jump` 가 **2장뿐**이다 — 이륙과 체공. 하강 포즈가 없어서 `FallState` 가 체공 프레임을
붙잡아 쓴다. 상승과 하강이 같은 그림이 된다.

넣으려면 프레임을 직접 그려야 하고(6종 × 1장), 컨트롤러에 `Fall` 상태도 추가해야 한다.
**베이스 컨트롤러는 6종이 공유**하므로 오버라이드마다 클립을 지정하는 작업이 붙는다.
1차에서는 넣지 않고 W6 폴리시 항목으로 둔다 — 되돌리기가 싸고 레벨 전제에 안 박힌다.

#### 클립이 전부 루프로 설정돼 있다

`Death` 만 빼고 9개 클립 모두 `m_LoopTime: 1` 이다. 일회성 클립(공격 4종 · `Hit` · `Jump`)
까지 루프라서, 모션이 코드 타임라인과 무관하게 자기 주기로 돈다.

**굽고 나면 루프 플래그를 끈다** — 굽기 절차의 일부다 (10.2). 다시 구우면 초기화된다.
`Idle` 과 `Run` 은 루프가 맞으므로 그대로 둔다.

#### 빌드하려면 `using UnityEditor;` 를 감싸야 한다

Pixem 스크립트 세 개가 **`using UnityEditor;` 를 `#if UNITY_EDITOR` 밖에 두고 있다.**
클래스 본문은 가드 안인데 `using` 만 나와 있다.

| 파일 | `using` | 가드 블록 |
|---|---|---|
| `Pixem/Scripts/PixemCharacter.cs` | 4행 | 15–146 |
| `Pixem/Scripts/PixemManager.cs` | 6행 | 119–976 |
| `Pixem/Scripts/PixemUIManager.cs` | 3행 | 73–136 |

**에디터에서는 아무 문제가 없고 빌드에서만 `CS0246` 이 난다.** 플레이어 빌드에는
`UnityEditor` 어셈블리가 없기 때문이다. 이 스크립트들은 asmdef 가 없어 `Assembly-CSharp`
로 들어가므로 플레이어 빌드의 컴파일 대상이다.

각 `using` 을 `#if UNITY_EDITOR` / `#endif` 로 감싸면 끝난다. **에셋을 다시 임포트하면
되돌아가므로** 굽기 절차와 같은 성격의 반복 작업이다.

`using UnityEngine;` 은 런타임에도 필요하다. 헤더를 통째로 바꾸다 이 줄을 날린 적이
있으니, 고친 뒤 `git -C Assets/Imported diff` 로 의도한 줄만 바뀌었는지 본다.

### 10.2 무기 교체는 파츠가 아니라 프리팹 단위로 한다

`PixemCharacter` 의 조립 코드는 **전체가 `#if UNITY_EDITOR`** 로 감싸져 있다.
`AnimationUtility` 가 에디터 전용 API 이기 때문이다. 즉 **런타임에 무기 스프라이트만
갈아끼우는 것은 불가능하다.**

대신 조립 결과가 독립적인 에셋 세트로 떨어진다.

```
Modified/Resources/CharacterPrefabs/Character_N.prefab
Modified/Resources/CharacterResources/Character_N/
    Animations/  구워진 클립 9개 + 컨트롤러
    CharacterData_N.asset
    Character_N_Sheet.png        ← 파츠가 통합된 시트
```

스프라이트가 시트로 합쳐져 원본 `Pixem/Resources/Items/` 에 의존하지 않는다. 그래서
**무기별로 캐릭터를 미리 구워두고, 런타임에는 오버라이드 컨트롤러를 교체한다.**

이 방식이 3장의 의도에 오히려 더 맞는다. 무기가 바뀌면 클립 세트가 통째로 바뀌므로
베기와 찌르기가 모션으로 갈린다. 무기 SO 에 컨트롤러 참조를 넣으면 9장 확장
지점 1(무기 SO 래퍼)이 그대로 커버한다.

**대가**: 대기 중인 무기를 등에 멘 모습처럼 **두 무기를 동시에 보여줄 수 없다.**
1차에서는 UI 로 표시한다.

##### 정정 — 프리팹은 갈아끼우지 않는다

처음에는 "프리팹·애니메이터를 교체한다"고 적었다. **프리팹은 필요 없다.**

굽힌 6종의 `_Modified.controller` 는 전부 `AnimatorOverrideController` 이고,
**베이스가 하나로 같다** (`Imported/Pixem/Animation/PixemAnimator.controller`).
베이스가 같으면 상태 이름·전이·파라미터가 동일하므로 클립만 갈리면 된다.

```
animator.runtimeAnimatorController = 무기SO의_오버라이드
```

클립이 자기 시트의 스프라이트를 들고 있으므로 외형도 따라온다. 반대로 **`SpriteRenderer`
에 스프라이트를 대입하는 것만으로는 안 된다** — 애니메이터가 매 프레임 그 값을 덮어쓴다.

프리팹을 갈아끼우면 자식이 파괴돼 `PlayerMovement.animator` · `Invincibility.sprite`
참조가 끊긴다. 컨트롤러만 갈면 그 문제 자체가 생기지 않는다.

**6종 시트의 피벗이 전부 `{0.6875, 0.125}` 로 같다는 것도 확인했다.** 캐릭터 자식이
하나뿐이어도 무기별로 `localPosition` 오프셋을 다시 잡을 일이 없다.

#### 굽기 절차

도구가 만들어주는 데서 끝나지 않는다. **아래 넷은 매번 손으로 해야 한다.**

1. `Assets/Pixem/Resources/` 에 떨어진 결과물을 `Assets/Imported/Modified/Resources/` 로 옮긴다
   (`CharacterPrefabs` 는 `Resources.LoadAll` 대상이므로 `Resources` 아래를 벗어날 수 없다)
2. 프리팹과 리소스 폴더 이름을 **무기명으로 바꾼다.** 도구는 `Character_N` 번호로 만드는데,
   번호로는 어느 무기인지 열어봐야 알 수 있다 — 실제로 프리팹 목록에서 잘못 집는 사고가 났다
3. **일회성 클립의 루프를 끈다** — 공격 4종 · `Hit` · `Jump`. 안 끄면 콤보에서 모션이
   판정과 어긋난다 (10.1)
4. **`Jump` 클립의 키프레임을 재구성한다** — 아래

무기를 늘릴 때마다 반복된다. 1차에서는 검만 필요하므로 나머지 5종은 W2 에서 해당 무기를
붙이는 시점에 처리한다.

##### `Jump` 재구성 — 검에만 되어 있다

구워진 원본은 키프레임 3개다. 스프라이트가 둘뿐인데(10.1) 루프를 위해 마지막이 반복된다.

```
원본 (창·활·단검·지팡이·기본형)   A(0) → B(0.1333) → B(0.2667)   stop 0.2833  loop 1
검                                B(0)  → A(0.15)                  stop 0.1667  loop 0
```

`FallState` 가 `PlayClip(clipJump, normalizedTime: 1)` 로 **마지막 프레임을 붙잡아** 하강
포즈로 쓴다. 그래서 어느 그림이 끝에 오는지가 곧 하강 포즈다 — 원본 순서로는 이륙 포즈가
끝에 남는다. 검은 순서를 뒤집어 체공 포즈가 끝에 오게 했다.

**루프만 꺼서는 안 된다.** 순서까지 바꿔야 `FallState` 가 의도한 그림을 잡는다.

#### 재생 속도는 파생값이다

콤보 단계마다 클립 재생 속도를 다르게 줘야 타격 프레임이 히트박스와 맞는다. 이 값을
직접 넣지 않고 **치켜드는 프레임의 길이 ÷ `startup`** 으로 계산한다.

맞춰야 할 대상이 클립 전체 길이가 아니라 `startup` 이라는 게 핵심이다. 클립을 단계 길이에
맞추면 멈춤은 사라지지만 치켜드는 프레임이 늘어나 **히트박스가 나갈 때 아직 칼을 들고
있다.** `recovery` 구간에 마무리 자세가 떠 있는 건 후딜의 무게로 읽히므로 문제가 아니다.

W2 에서 이 값(`clipWindup`)은 무기 SO 로 옮긴다. 클립에 종속된 값이라 클립 참조와 같은
자리에 있는 게 맞다. 3장의 "무겁게 = 느린 재생"이 이 필드다.

##### 실측 — 치켜드는 프레임의 길이는 무기와 무관하게 같다

처음에는 "무기마다 클립이 달라 값도 달라진다"고 적었다. **프레임 수에서 추론했고 재보지
않았다.** #46 에서 실제로 재보니 틀렸다.

| 클립 | 키프레임 시각 | 길이 |
|---|---|---|
| `AttackSlash` (3장) | 0 · **0.1167** · 0.2167 · 0.3 | 0.3167 |
| `AttackPrick` (4장) | 0 · **0.1167** · 0.2167 · 0.3167 · 0.4 | 0.4167 |
| `AttackShoot` (4장) | 0 · **0.1167** · 0.2167 · 0.4167 · 0.5 | 0.5167 |

검·창·활 세 캐릭터에서 완전히 동일하다. Pixem 이 같은 타이밍 템플릿으로 굽고 스프라이트만
갈아끼우기 때문이다.

**프레임 수가 다른 건 맞지만 프레임은 뒤에 붙지 앞이 늘어나지 않는다.** 그래서
`clipWindup` 은 **현재 6종 전부 `0.1167`** 이다.

그래도 필드는 무기 SO 에 둔다. 근거가 "무기마다 다르니까"에서 "**클립에 종속되니까**"로
바뀔 뿐이다 — 다시 구우면 초기화되고, 후속에 다른 타이밍 템플릿의 무기가 오면 달라진다.
**클립 길이는 실제로 다르므로** 재생 속도를 `startup` 에 맞추는 파생 계산은 그대로 의미가
있다.

---

## 11. 설정 / 언어 / 저장

"세이브"를 한 덩어리로 다루면 판단이 꼬인다. **성격이 완전히 다른 세 가지**로 나눈다.

| 종류 | 대상 | 1차 포함 | 비용 |
|---|---|---|---|
| **설정 저장** | 볼륨, 해상도, 전체화면, 키 바인딩, 언어 | ✅ 포함 | 낮음 |
| **런 중간 저장** | 진행 중인 런을 중단·재개 | ❌ 이연 (자리는 남김) | 높음 |
| **메타 진행 저장** | 영구 강화, 해금 (7장) | ❌ 이연 | 중간 |

---

### 11.1 설정 (1차 포함)

포트폴리오에서 **옵션 화면이 없는 게임은 미완성으로 읽힌다.** 비용도 낮아 1차에 넣는다.

- 항목: 마스터/BGM/SFX 볼륨, 해상도, 전체화면, 언어
  (**키 설정은 1차 제외** — 11.4 참조)
- 저장 위치: JSON 파일 (`Application.persistentDataPath`). PlayerPrefs 는 항목이 늘면 관리가 어렵다
- **런타임 상태와 영구 상태를 섞지 않는다** — 설정은 `GameSettings` 하나에 모으고,
  이후 메타 성장 세이브가 붙어도 파일을 분리해 둔다

### 11.2 언어 / 로컬라이즈 — **1차: 한국어 + 영어**

> **앞선 판단 정정:** 로컬라이즈를 "나중에 붙여도 재작업 없는 영역"으로 분류했으나 이는 틀렸다.
> UI 문자열을 하드코딩해두면 나중에 전부 찾아 교체해야 한다. **나중에 하면 비싸지는 축**이다.

- **1차 지원 언어: 한국어 + 영어** (확정)
- 모든 UI 문자열을 **키로 참조**하고, 두 언어 테이블을 채운다
- **후속: 언어 추가 = 테이블에 열 추가.** 코드 수정 없음
- 방식: Unity Localization 패키지(`com.unity.localization` 1.5.12) 의 **String Table**
- 무기·유물·적 이름/설명도 SO에 직접 문자열을 박지 말고 **키를 들고 있게** 한다

**테이블 구성** (`Assets/_Project/Localization/`)

| 테이블 | 담을 것 | 쓰는 쪽 | 상태 |
|---|---|---|---|
| `UI` | 메뉴, 버튼, 설정 화면, 결과 화면 | `Game.UI` | 생성됨 |
| `Content` | 무기·유물·적 이름과 설명 | `Game.Gameplay` (SO 정의) | [#45](../../issues/45) 에서 생성 |

문자열의 **주인이 누구인지**로 나눈다. 하나로 몰아도 동작하지만 100개를 넘어가면 찾기 어려워진다.

> **정정 — SO 정의는 `Game.Core` 가 아니라 `Game.Gameplay` 다.** `WeaponData` 는 `AttackData`
> 를 들고 있고 그건 `Game.Gameplay/Combat` 에 있다. 의존 방향이 `Core ← Gameplay` 이므로
> `Game.Core` 에 두면 **역류한다.** `Game.Core` 는 지금 상태머신 베이스만 있는 도메인 무관
> 자리이고, 무기는 도메인 개념이라 애초에 여기가 맞다.

> 1차 텍스트 분량이 적을 때(50~100개) 구조를 잡는 게 가장 싸다. 콘텐츠가 늘어난 뒤엔 몇 배가 된다.

### 11.3 런 중간 저장 — 로그라이크에 필요한가

**결론: 상용 기준으로는 필요하다. 다만 1차 범위 밖으로 이연하고, 붙일 자리만 남긴다.**

필요한 이유는 흔히 오해하는 "세이브 스커밍"이 아니라 **중단**이다. 1런이 20~30분이면
플레이어가 중간에 자리를 떠야 하는 상황이 반드시 생긴다. 그래서 상용 로그라이크
(하데스, 슬레이 더 스파이어, 스컬)는 거의 예외 없이 **이어하기**를 제공한다.

동시에 스커밍은 막아야 하므로, 업계 표준 방식은 이렇다:

- **슬롯 1개만** 유지
- **저장 = 게임 종료**, **로드 = 세이브 삭제** (delete-on-load)
- 즉 "중단 지점"이지 "체크포인트"가 아니다

**1차에서 빼는 이유:** 저장 시점을 방 안까지 허용하면 적 위치·체력·AI 상태·투사체까지
전부 직렬화해야 한다. 120시간 예산에서 감당할 작업이 아니다.

**나중에 싸게 붙이기 위해 1차에서 지킬 것:**

| 지킬 것 | 이유 |
|---|---|
| 런 상태를 `RunState` **하나의 순수 데이터**로 모은다 (MonoBehaviour 참조 금지) | 그대로 직렬화 대상이 됨 |
| `RunState` 에 담을 것: 플레이어 스탯, 무기 2슬롯, 유물 목록, 현재 방 인덱스, **RNG 시드** | 이것만으로 런 복원 가능 |
| 방 구성을 **시드 기반**으로 결정 | 방 배치를 저장할 필요가 없어짐 |
| 저장 지점을 **방 경계로만** 상정하고 설계 | 방 내부 상태를 직렬화할 필요가 사라짐 |

이 네 가지를 지키면 후속 작업은 **`RunState` JSON 덤프 + 로드 시 복원**이 전부다.
반대로 런 상태가 여러 MonoBehaviour에 흩어져 있으면 그때 가서 전면 재작성이 된다.

> `RunState` 는 7장의 `RunResult` 와 짝을 이룬다. 런 종료 시 `RunState` → `RunResult` 정산.

### 11.4 입력 — 장치 지원과 키 설정은 별개다

혼동하기 쉬우나 완전히 다른 작업이다.

| 항목 | 내용 | 1차 | 비용 |
|---|---|---|---|
| **키보드 + 게임패드 지원** | 두 장치를 모두 받는 것 | ✅ 포함 | 낮음 (~1h) |
| **키 설정 (리바인딩)** | 플레이어가 옵션에서 키를 바꾸는 것 | ❌ 이연 | 2~3h |

**장치 지원 (1차 포함)**
Input System 의 Control Scheme 으로 처리한다. 액션 맵 하나에 키보드·게임패드 바인딩을
두 벌 등록하면 끝이라 실질 추가 비용이 거의 없다. 숫자 키패드 등 추가 키도 바인딩 한 줄이다.

**키 설정 (이연)**
`PerformInteractiveRebinding` + 전용 UI + 중복 바인딩 충돌 검사 + 저장/로드가 필요하다.
1차 예산에서는 빼지만, **아래 한 가지만 지키면 후속 추가가 저렴하다.**

> **UI에 키 이름을 하드코딩하지 않는다.**
> `"E 키를 누르세요"` 처럼 박아두면 리바인딩을 붙일 때 전부 찾아 고쳐야 하고,
> 게임패드로 전환해도 키보드 표기가 그대로 남는다.
> `InputBinding.ToDisplayString()` / `GetBindingDisplayString()` 으로 **현재 바인딩에서 표시
> 문자열을 가져온다.** 그러면 장치 전환과 리바인딩이 자동 반영된다.
>
> 로컬라이즈와도 엮인다 — String Table 에는 `"{0} 키를 누르세요"` 형태로 넣고
> 키 이름은 런타임에 주입한다 (11.2).

후속 추가 시 저장은 Input System 이 제공하는
`SaveBindingOverridesAsJson()` / `LoadBindingOverridesFromJson()` 한 쌍으로 끝난다.

---

## 12. 미결정 사항 정리

| # | 항목 | 영향 | 결정 시점 |
|---|---|---|---|
| 1 | 타일셋 확보 (32px 기준) | 방 프리팹 치수 | **W3 착수 전** — [#44](../../issues/44) 로 착수, W2 와 병행 |
| 2 | 업적을 실제로 넣을지 | 저장 포맷 · UI 물량 | **후속 5번(메타 성장) 착수 직전** |
| 3 | 2인 코옵(UGS) 착수 여부 | 후속 일정 전체 | **후속 1~2번(콘텐츠·스테이지 2) 완료 후** |
| 4 | 접촉 피해를 둘지 | 간격 압박 · 적 배치 | **W3 (적 3종 설계 시)** |

접촉 피해는 #16 에서 몸통을 통과시키면서 생긴 숙제다. 물리가 "다가가면 위험하다"를 말해주지
않으므로 규칙으로 표현해야 한다.

**#17 에서는 넣지 않는다.** 완료 조건이 "예고를 보고 피할 수 있다"인데, 접촉 피해가 있으면
맞았을 때 원인이 둘이 된다 — 공격에 맞았는지 몸에 닿았는지. **검증하려는 것이 흐려진다.**

W3 으로 미루는 이유는 판단 재료가 지금 없기 때문이다. 더미 하나로는 "접촉이 위험해야
하는가"를 알 수 없고, 적 3종(돌진·원거리·방패)이 서로 다른 거리를 두고 싸울 때 비로소
간격 압박이 필요한지가 드러난다. 돌진형에만 두는 선택지도 그때 생긴다.

붙이는 비용은 낮다. 적 몸통에 `HitBox` 를 상시 켜두고 `EnemyHitbox` 레이어를 주면 되므로
지금 남길 것은 없다.

1번은 결정이 아니라 실행이라 [#44](../../issues/44) 로 옮겼다. W1 종료 시점에 표를 훑어
**W2 와 병행하기로 정했다** — W2 는 코드 작업이라 에셋 탐색과 겹치지 않고, 후보를 미리
골라두면 마음에 안 들 때 다시 찾을 시간이 남는다. 캐릭터 확정이 W1 중반까지 밀렸던 것과
같은 실패를 반복하지 않기 위해서다.

동일 무기 2개(2번)는 슬롯이 `WeaponInstance` 둘이라 **같은 `WeaponData` 를 가리키되 쿨타임은
따로 도는 게 기본 동작**이다 (#47). 금지하는 쪽이 오히려 검사를 추가하는 일이라 미루는
비용이 0 이다.

업적·코옵(3·4번)은 1차 범위 밖이라 지금 정하지 않는다. 다만 **결정 시점이 오면 그때 물어야 한다** —
그 전에 착수하면 콘텐츠가 빈 채로 시스템만 있는 상태가 되고, 지나쳐 버리면 이미 짜둔
저장·이벤트 구조를 다시 손대게 된다. 판단 근거는 8장(업적)과 8.1(코옵)에 있다.

### 결정 완료

| 항목 | 결정 | 근거 |
|---|---|---|
| 1차 지원 언어 | **한국어 + 영어** | 11.2 |
| 키 설정 (리바인딩) | **이연** — 장치 지원만 1차 포함 | 11.4 |
| 아트 파이프라인 | 구매 에셋이 제공하는 방식을 따름 | 10장 |
| 1차 아트 에셋 | **Pixem — 1차 한정.** 여유가 생기면 교체하되 W1~W6 중에는 바꾸지 않음 | 10.1 |
| 무기 스킬 · 대기 쿨타임 | **이연 — 후속 0순위.** 1차는 평타만 | 3장 · 8장 · 로드맵 |
| 런 중간 저장 | 이연 — `RunState` 구조만 확보 | 11.3 |
| 2단 점프 | **없음** — 1단 유지 | 아래 12.1 |
| 멀티플레이 | **PvP 는 범위 밖.** 2인 코옵(UGS)만 여지를 남김 | 8.1 |
| 유료 에셋 | **Pixem** (파츠 조합형 픽셀 캐릭터). PPU 32 · 2유닛 | 10.1 |
| 무기 교체 표현 | 파츠 교체가 아니라 **오버라이드 컨트롤러 교체** | 10.2 |
| 화면 흔들림 | **피격에만.** 타격 쪽은 0 | 아래 12.2 |
| 지상 공격 중 전진 | **넣는다 — 조금.** 구현은 W2 | 아래 12.3 |
| 스왑에 공격 판정 | **주지 않는다** — 회피 + 교체만 | 아래 12.4 |
| 공격 중 스왑 | **허용한다** — 공격이 취소된다 | 아래 12.4 |
| 무기 공격 방식 분기 | **`WeaponData` 의 `AttackKind` enum** | 아래 12.5 |
| `activeTime` 이름 | **안 바꾼다** — 관찰이 쌓일 때까지 | 아래 12.5 |
| 동일 무기 2개 장착 | **허용한다** — 막지 않는다 | 아래 12.6 |
| 방 = 씬 vs 프리팹 | **프리팹** — 한 씬에 배치 | 아래 12.7 |

### 12.1 2단 점프를 넣지 않는다 ([#8](../../issues/8))

W1 시점에 1단 점프의 체감을 확인한 결과 체공 시간과 낙하 속도가 충분했다. 도달 범위가
부족하면 `jumpHeight` 를 올려 해결한다 — 점프 파라미터를 속도가 아니라 **높이 단위**로
노출해뒀기 때문에 레벨 치수와 직접 비교할 수 있다.

**되돌리기 비용이 비대칭이라는 게 결정적이었다.**

| | 비용 |
|---|---|
| 1단으로 설계 → 나중에 2단 추가 | 싸다. 기존 방이 전부 통과 가능해지고 난이도만 내려간다 |
| 2단으로 설계 → 나중에 제거 | 비싸다. "2단으로만 닿는 높이"가 레벨 전체의 전제가 되어 방을 다시 그려야 한다 |

**대가: 공중 기동이 스왑 대시 하나로 몰린다.** W3 방 설계에 제약이 생긴다 —
**스왑 쿨다운 중에도 통과 가능해야 한다.** 어기면 쿨다운 동안 갇히는 방이 나온다.

#### 나중에 추가한다면 (이연 방법)

1. `PlayerMovement` 에 남은 점프 횟수 카운터. 접지 시 리셋
2. `TryJump` 의 거부 조건에서 **접지·코요테 검사와 횟수 검사를 분리**한다.
   공중에서 첫 점프를 쓴 경우(코요테 소진 후 낙하 중)를 어떻게 셀지가 실제 쟁점이다
3. **`machine.Change(Jump)` 가 무시된다** — 이미 `Jump` 상태면 `StateMachine` 의
   같은-상태-무시 규칙에 걸려 `Enter()` 가 다시 돌지 않는다. 공중 점프 모션이 재생되지
   않는다. `DoubleJumpState` 를 별도로 두거나 `Change(state, force: true)` 경로가 필요하다.
   학습 로그 5절에서 예외로 언급한 `force` 가 실제로 필요해지는 첫 사례다

### 12.2 화면 흔들림은 피격에만 둔다 ([#17](../../issues/17))

```
플레이어 공격  shakeStrength  0
적 공격        shakeStrength  0.03
```

**피격 흔들림은 정보고 타격 흔들림은 연출이다.** "내가 맞았다"를 알리는 건 취향으로 뺄 수
없는 기능이지만, 타격 쪽은 없어도 게임이 성립한다. 히트스탑과 넉백이 이미 타격을 전달하고
있어서 흔들림이 더할 게 적다.

접근성 측면에서도 흔들림 총량이 줄어드는 편이 낫다. 멀미를 느끼는 사람이 실제로 있다.

**#14 에서 정하지 않고 여기까지 미룬 이유**는 그때 피격 흔들림을 겪어본 적이 없었기
때문이다. 적이 공격하지 않는 상태에서의 선호는 관찰이 아니라 예측이다. 미루는 비용이
0 이었으므로 — 강도가 `DamageInfo` 를 타고 오도록 설계해서 **바꾸는 게 값 두 개**였다 —
판단이 설 때까지 기다렸다.

W5 설정 화면에 강도 슬라이더를 둔다. 값이 이미 데이터라 UI만 붙이면 된다.

**되돌리기 비용은 여전히 값 하나다.** 타격 쪽이 밋밋하다고 느껴지면 플레이어
`AttackData.shakeStrength` 를 올리면 된다. W6 손맛 재조정에서 다시 볼 수 있다.

---

### 12.3 지상 공격에 짧은 전진을 넣는다 — 구현은 W2 ([#17](../../issues/17))

적이 추격하고 물러나는 것을 보고 정했다. 콤보 도중 제자리에 박혀 있는 것보다 조금
따라붙는 편이 리듬이 이어진다.

**#16 에서 넉백을 낮춘 것이 이 판단을 바꿀 뻔했다.** 1·2타를 5 → 3 으로 내려 3타가
사거리 안에 남게 만들었으므로 "전진이 없으면 콤보가 안 된다"는 근거는 사라졌다. 남은 건
필수가 아니라 **감각**이다 — 그래서 "조금"이다.

#### 구현이 W2 인 이유

`AttackData` 배열이 W2 에서 무기 SO 로 옮겨간다. 지금 `forwardSpeed` 를 넣으면 **그 필드를
곧 다시 옮기게 된다.** 무기마다 전진 거리가 달라야 하므로(창은 길게, 대검은 짧게) 어차피
무기 SO 의 필드다.

#### 통로는 이미 있다

지상 공격은 `AllowsHorizontalControl` 이 `false` 라 `targetSpeed` 가 0 으로 눌린다. 속도를
그냥 넣으면 `groundDecel` 35 에 즉시 깎인다 — `forwardSpeed` 2 면 0.06초, 이동거리 0.06유닛.
몸통 0.7 에 비하면 안 움직이는 것과 같다.

[#13](../../issues/13) 이 만든 **"코드가 부여한 속도가 `HandleHorizontal` 에 눌리지 않는
구간"** 이 그대로 쓰인다. `Knockback` 컴포넌트가 하는 일이 정확히 그것이다 — 짧은 속도
부여 + 감쇠 + 제어 차단.

| | 방법 | 대가 |
|---|---|---|
| A | `Knockback.Apply(앞쪽 벡터)` 재사용 | 새 코드 0. **이름이 거짓말**을 한다 |
| B | `VelocityImpulse` 등으로 일반화 | 이름이 정직해진다. 기존 배선·필드명을 다 고쳐야 한다 |

**B 를 W2 에서 무기 SO 이전과 함께 한다.** 어차피 그때 이 근처를 건드리므로 비용이 겹친다.
지금 A 로 넣으면 이름이 어긋난 채로 굳는다.

#### 구현 결과 (#50)

`Knockback` → `VelocityImpulse`. 다만 **`AttackData.knockbackForce` 와 `DamageInfo.Knockback`
은 이름을 그대로 뒀다** — 그건 실제로 넉백이다. 통로만 일반화하고 개념은 남긴다. 전부
바꿨다면 "때렸을 때 미는 힘"을 부를 이름이 사라졌을 것이다.

`Apply(impulse, duration)` 로 **호출별 지속시간을 받게** 했다. 하나의 `knockbackTime` 으로는
소비자 넷(피격·적·스왑 대시·공격 전진)을 못 맞춘다. `remaining` 만으로는 감쇠 분모가
고정되므로 `total` 을 함께 기록한다.

**전진은 세 타 모두에 같은 값으로 준다.** 1·2타는 제자리인데 3타만 훅 들어가면 "왜 지금만"
이 된다 — 리듬이 일정한 쪽이 낫다. 그래서 `forwardSpeed` 는 `AttackData` 가 아니라 **무기 SO
필드 하나**로 충분하다.

**지속시간은 그 타의 `startup` 을 그대로 쓴다.** 새 필드가 생기지 않고, 치켜드는 동안 몸이
따라 들어가고 판정이 나갈 때는 멈춘다. `clipWindup ÷ startup` 으로 재생 속도를 뽑은 것과
같은 결 — **손으로 넣는 값을 하나 줄인다.**

시작값 `forwardSpeed 3`. 1타 기준 `3 × 0.12 ÷ 2 = 0.18유닛`, 몸통(0.7)의 4분의 1 이다.

### 12.4 스왑은 회피와 교체만 한다 — 공격 판정은 주지 않는다 ([#47](../../issues/47))

대시를 실제로 굴려본 뒤 정했다. 굴려보기 전의 선호는 관찰이 아니라 예측이라 여기까지 끌었다
(화면 흔들림을 #17 까지 끈 것과 같은 이유).

**판정을 주지 않는 이유는 거리다.** 대시가 `dashSpeed 12` × `dashTime 0.2` ÷ 2 = **1.2유닛**
이라 적을 관통하지 못한다. 판정을 붙여도 맞히기 어렵고, 맞히려면 대시를 늘려야 하는데
**그러면 회피 거리로는 과해진다.** 한 값이 두 역할을 동시에 만족시키지 못한다.

역할이 하나면 값도 하나로 정해진다 — 대시 거리는 **"피할 수 있는가"만 보고** 튜닝하면 된다.

붙이는 비용은 나중에도 낮다. `SwapState.Enter` 에서 `HitBox` 를 켜면 되고, 지금 남겨둘
구조는 없다. 스왑 쿨타임이 3.3초라 **딜까지 붙으면 "가끔 쓰는 강한 기술"이 되어** 회피
타이밍에 아껴 쓰는 판단이 흐려지는 것도 이유다.

#### 공격 중 스왑은 허용한다 — 공격이 취소된다

`AttackState.AcceptsSwapInput` 이 `true` 다. 후딜에 갇혀 맞는 것을 스왑으로 끊을 수 있다.
[4장](#4-코어-게임플레이-루프)의 "스왑은 회피기이기도 하다"가 성립하려면 **정작 위험한
순간인 공격 후딜에 쓸 수 있어야** 한다.

이 결정이 `AttackState.Exit()` 의 `cts.Cancel()` 을 **#10 이후 처음 실제로 돌린다.**
그래서 검증 항목이 하나 따라온다 — 공격 도중 스왑했을 때 `finally` 가 히트박스를 끄는가.
안 끄면 켜진 채로 남아 스왑 후에도 적을 때린다.

#### 스왑 i-frame 은 깜빡이지 않는다

깜빡임은 12.2 와 같은 축의 문제다. **깜빡임은 "맞았다"의 신호로 만들었으므로**
(#15) 스왑에도 쓰면 플레이어가 피격으로 오독한다. 대시가 앞으로 나가는 동작이라
"맞아서 밀렸나"로 읽히기 더 쉽다.

`Invincibility` 가 무적 시간과 깜빡임을 함께 들되 **`Begin(time, blink)` 로 갈랐다.**
겹칠 때는 `blinking || blink` — 스왑 무적 중에 맞으면 깜빡이기 시작하고, 피격 무적 중에
스왑해도 깜빡임이 유지된다. **"맞았다"가 더 중요한 정보**라 한쪽이 요구하면 이긴다.

타이머는 여전히 하나다. 늘어난 것은 플래그지 시간이 아니다.

스왑 무적은 이제 화면에 표시가 없다. **대시 동작 자체가 신호**이고, 잔상이나 섬광은
대시 전용 그림이 없어서 W6 폴리시로 둔다.

### 12.5 공격 방식 분기는 `WeaponData` 의 enum 이 정한다 ([#49](../../issues/49))

#48 에서 투사체를 만들다 드러났다. **활은 `HitBoxActivate` 를 아예 부르지 않아야 한다** —
켜고 크기를 0 으로 줄이는 우회는 `target` 초기화와 물리 프록시 생성이 그대로 남는다.
근접과 원거리는 `AttackState` 타임라인의 **같은 자리에서 다른 줄로 갈라진다.**

```
startup 대기 후
  근접    HitBox.HitBoxActivate(...)  →  activeTime  →  Deactivate()
  원거리  FireProjectile(data)           켤 히트박스가 없다
```

갈림길을 **`AttackState` 가 알면 안 된다.** 상태가 무기 종류를 묻기 시작하면 무기가 늘 때마다
상태가 는다 — 9장 확장 지점 1(무기 SO 래퍼)이 "데이터 작성"으로 남으려면 무기 SO 가 정해야
한다. `RangedAttackState` 를 따로 두는 안이 여기서 탈락한다. 콤보·취소 로직을 복제하게 된다.

**`null` 검사로 가르지 않는다.** `projectilePrefab != null` 이면 원거리로 하면 필드가 안 늘지만,
**활 SO 에서 배선을 빠뜨렸을 때 조용히 근접이 된다.** 의도적 공백과 실수가 같은 모양이 되는
것은 #15 에서 폴백을 걷어낸 이유와 같다. enum 이면 "원거리인데 프리팹이 없다"를 경고로 잡을
수 있다.

**전략 객체(`AttackExecutor` SO 서브클래스)는 지금 만들지 않는다.** 새 공격 방식이 데이터
작성이 되는 건 맞지만, SO 체계와 `AttackState` 가 넘길 컨텍스트를 **지금 정의해야 한다.**
완드가 범위 논타게팅으로 갈지 투사체로 갈지가 아직 미확정이라(9장), 컨텍스트의 모양을 정할
근거가 없다.

**enum 에서 전략 객체로 가는 길은 막히지 않는다.** 값이 셋이 되고 각 `case` 가 커지면 그때
뽑는다. 중복이 실제로 보인 다음에 추상화한다.

#### `activeTime` 의 이름은 바꾸지 않는다

근접에서는 "판정이 나가 있는 시간"인데 원거리에서는 발사가 순간이라 그 뜻이 없다. 타임라인
구간으로는 여전히 필요하므로 **원거리에서는 "발사 후 경직"으로 재해석해 그대로 쓴다.**

12.3 의 `knockbackForce` 와 반대 방향이다. 그때는 이름이 **여전히 정확해서** 남겼고, 여기서는
**한쪽에서만 정확하다.** 그래도 안 바꾸는 이유는 대안이 더 나쁘기 때문이다 — 중립적인 이름으로
바꾸면 근접에서 정확했던 것까지 잃는다.

원거리 무기가 활 하나뿐이라 근거가 얇고, #51(무기 스킬)이 들어오면 구간 구조 자체가 다시
흔들릴 수 있다. **미루는 비용이 0 이면 관찰이 쌓일 때까지 미룬다.**

---

### 12.6 동일 무기 2개 장착을 막지 않는다

**코드를 한 줄도 안 썼다. 이미 동작한다.**

```csharp
slots[i] = new WeaponInstance(startingWeapons[i]);
```

`WeaponHolder` 가 슬롯마다 **독립된 `WeaponInstance`** 를 만들고 쿨타임이 그 인스턴스의
필드라, 같은 `WeaponData` 를 둘 다 넣어도 쿨타임이 따로 돈다.

9장 확장 지점 1번(**무기 인스턴스를 SO 직접 참조가 아닌 런타임 래퍼로**)이 값을 한 지점이다.
SO 를 직접 들었다면 두 슬롯이 같은 객체를 가리켜 **쿨타임이 공유**됐을 것이다 — 스왑하는
순간 양쪽이 다 잠긴다. #45 에서 래퍼를 넣어둔 덕에 이번 결정이 "막지 않는다" 한 줄로 끝났다.

**되돌리기 비용이 비대칭이다.**

| | 비용 |
|---|---|
| 지금 열어둔다 | 0. 스왑이 회피 대시로만 기능하는 빌드가 하나 생긴다 |
| 지금 막는다 | 검사 한 줄. 후속에서 그 줄을 지우고 밸런스를 다시 본다 |

12.1(2단 점프)에서 쓴 것과 같은 기준인데 방향이 반대다. 그때는 **넣었다 빼는 쪽이 비쌌고**
여기서는 **막았다 여는 쪽이 비싸다.**

결정적인 것은 **스킬이 이연됐다는 사실**이다. 스킬이 붙으면 동일 무기 2개는 "같은 스킬을 두 번
쓰는" 빌드가 되어 4장의 쿨타임 순환과 정확히 맞물린다. 지금 막으면 그때 다시 열고 밸런스를
처음부터 본다.

**1차에서는 재미없는 선택지다.** 스킬이 없으니 평타가 같은 무기 둘이고, 스왑은 회피 대시로만
기능한다. 그래도 막지 않는 이유는 위와 같다 — **나쁜 선택지를 금지하는 것과 존재하지 않게
하는 것은 다르다.**

---

### 12.7 방은 씬이 아니라 프리팹이다 — W3 착수 전

6장이 "방은 **수제작 프리팹 풀에서 랜덤 추출**"이라고 적었지만 그건 **절차 생성이냐 수제작이냐**를
정한 것이고, **런타임에 프리팹이냐 씬이냐**는 안 정했다. G1(방 데이터 + 프리팹 구조)이 이 위에
서므로 착수 전에 갈랐다.

| | 한 씬 + 방 프리팹 | 방 = 씬 (Additive) |
|---|---|---|
| 전환 | `Instantiate` / `Destroy` | `LoadSceneAsync` / `Unload` |
| 플레이어·UI·`RunState` | **그냥 산다** | `DontDestroyOnLoad` 또는 부트스트랩 씬 |
| 인스펙터 배선 | 정상 | **씬 간 참조 금지** → 런타임 조회 |
| 두 방 동시 유지 | 쉽다 | 로드 타이밍 관리 필요 |

**프리팹으로 간다.** 근거가 셋이다.

**방이 작다.** 카메라 `orthographic size 2.5` 면 한 화면이 9 × 5 유닛이다. 방이 그 두세 배여도
프리팹 하나로 충분하고 씬 로딩이 아낄 메모리가 없다. 씬 단위가 값을 하는 건 한 구역이 메모리에
다 안 올라갈 때인데 2D 픽셀아트에서는 오지 않는다.

**씬 간 참조 금지가 크다.** 유니티는 에디터에서 씬을 넘는 참조를 허용하지 않는다. 방 씬의 스폰
포인트가 `RunState` 를 알거나 문이 스테이지 매니저를 알아야 하는데, 전부 런타임 조회가 된다.
이 프로젝트는 이미 **배선이 조용히 끊기는 사고**를 여러 번 겪었다(#50 의 무적 공유, #66 의
경고만 하고 터지던 것들). 인스펙터 배선을 런타임 조회로 바꾸면 그 유형이 늘어난다.

**문을 통과하는 연출이 열린다.** 두 방을 동시에 살려두고 카메라를 옮기는 것이 프리팹이면 그냥
된다. 씬이면 로드 완료를 기다려야 하고 그동안 플레이어를 어떻게 할지가 또 문제다.

#### "이어져 보이는 것"은 프리팹으로도 된다

방 프리팹을 **월드 좌표로 나란히 배치**하면 실제로 이어진다. 문을 지나면 카메라가 옆 방으로
팬된다 — 스컬이 그 방식이다. 4장 루프가 순차 진행이라 되돌아가지 않으므로 **지나간 방은 파괴**해도
되고, 그러면 메모리도 안 쌓인다.

#### 잃는 것

씬별 라이팅·포스트프로세싱 설정을 못 쓴다. 스테이지마다 분위기를 통째로 다르게 하려면 씬이 편하다.
지금은 `Global Light 2D` 하나뿐이고 1차에 스테이지가 여러 개도 아니다. 필요해지면 **방 프리팹 안에
Light 2D 를 넣는 것**으로도 된다.

#### 뒤집을 조건과 비용

- 방 하나가 **화면 20배 이상**으로 커진다 (메트로배니아식 연결 맵)
- 스테이지마다 **셰이더·포스트프로세싱이 통째로 다르다**
- 로딩 화면을 **의도적으로** 보여주고 싶다

셋 다 1차에 없다. **"일단은"으로 정한 것이 성립하는 이유는 되돌리기 비용이 방 내용물에 있지
않기 때문이다** — 방 프리팹은 씬에 그대로 넣을 수 있고, 다시 만들 것은 **방을 잇는 코드와 배선
모델**뿐이다. 비싼 것은 G1 의 방 규격(입구·출구·스폰 포인트·카메라 경계)이고 그건 어느 쪽이든
같다.


> 무기 등급제·이벤트방 등 나머지 항목은 8장에서 **이연**으로 정리했으므로
> 1차에서는 결정할 필요가 없다. 확장 지점만 남겨두면 된다.

---

## 관련 문서

- [개발 로드맵](roadmap.md)
- [학습 로그](learning-log.md) — 작업 중 막혔던 지점과 그때 이해한 것들
