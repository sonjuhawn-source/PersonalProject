# 테스트 케이스

> 최종 수정 2026-09-23 · 1차(2026-09-03 제출 빌드) 기준 · 일부 항목 자동화됨 ([#141](../../issues/141))
>
> 관련: [빌드 노트·결과](builds/) · [로드맵](roadmap.md) · [학습 로그](learning-log.md)

---

## 이 문서의 출처

**새로 만든 목록이 아니다.** 주차별 [빌드 계획](builds/)의 검증 체크리스트와
[빌드 결과](builds/)에 적힌 확인 항목을 TC 양식으로 옮긴 것이다.

그래서 `결과` 열은 추정이 아니라 **그 빌드에서 실제로 나온 값**이다. 확인하지 못한
항목은 통과로 적지 않고 △ 로 둔다 — [W3 결과](builds/3주차%20빌드%20결과.md)의
*"확인 못 했다와 안 된다를 섞지 않는다"* 가 이 열의 규칙이다.

### 무엇을 인게임 TC 로 두는가

**보이거나 안 보이거나, 막히거나 넘어가는 것만 둔다.** 시간·확률·수치는 플레이로
판정할 수 없으므로 [자동화 대상](#자동화-대상)으로 내린다.

무적 0.3초를 예로 들면, 인게임 TC 는 *"스왑 대시 중 적 공격에 체력이 안 깎인다"*
까지만 묻는다. 0.3초인지 0.6초인지는 코드 테스트가 답할 질문이다.

### 표기

| | |
|---|---|
| ✔ | 통과 |
| ✘ | 실패 — 수정함 |
| ⏸ | 실패 — 이연 (근거는 비고) |
| △ | 확인 못 함 |
| ⚙ | **자동 테스트가 판정한다** — 사람이 다시 안 본다 ([자동화된 것](#자동화된-것)) |

`선행 조건` 은 기본이 *"새 런 시작"* 이고, 다른 경우만 적는다.
재현이 필요한 항목은 `StageRunner.seed` 를 고정한다 — 런 시작 시
`런 시작 — 시드 N` 이 콘솔에 찍히므로 같은 런을 다시 만들 수 있다.

---

## 1. 이동 · 점프 (MOVE)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-MOVE-01 | 좌우 이동 | 입력 방향으로 이동하고 스프라이트가 뒤집힌다 | W1·W2 회귀 | ✔ |
| TC-MOVE-02 | 가변 높이 점프 | 버튼을 짧게 떼면 낮게, 끝까지 누르면 높게 뛴다 | W1·W2 회귀 | ✔ |
| TC-MOVE-03 | 코요테 타임 | 발판을 떠난 직후에 눌러도 점프가 나간다 | W1·W2 회귀 | ✔ |
| TC-MOVE-04 | 점프 버퍼 | 착지 직전에 눌러도 점프가 씹히지 않는다 | W1·W2 회귀 | ✔ |
| TC-MOVE-05 | 낙하 복귀 | 구멍에 떨어지면 마지막 접지 지점으로 돌아온다 | W4 완주 | ✔ |

## 2. 전투 · 피격 (CMB)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-CMB-01 | 3타 콤보와 선입력 | 연속 입력이 다음 타로 이어진다 | W2 회귀 | ✔ |
| TC-CMB-02 | 콤보 타임아웃 | 입력이 끊기면 1타부터 다시 시작한다 | W2 회귀 | ✔ |
| TC-CMB-03 | 히트스탑 · 넉백 · 화면 흔들림 | 타격 순간 셋이 함께 난다 | W2 회귀 | ✔ |
| TC-CMB-04 | 피격 무적과 깜빡임 | 맞으면 깜빡이고 그동안 다시 안 맞는다 | W2 회귀 | ✔ |
| TC-CMB-05 | 적 둘 사이 연속 피격 | 무적 중에 두 번째 적에게 이어서 안 갈린다 | W2 회귀 | ✔ |
| TC-CMB-06 | 지상 공격 중 전진 | 공격하면서 조금 앞으로 나간다 | W2 신규 | ✔ |
| TC-CMB-07 | 적 공격이 체력을 깎는다 | 맞으면 HUD 체력이 줄어든다 | W3 결과 | △ |
| TC-CMB-08 | 공중 공격 후 착지 | 착지 후에는 공격 중 이동이 막힌다 | 최종 결과 5 | ⏸ |

- **TC-CMB-07** — W3 빌드에서 `HurtBox.health` 가 비어 있어 맞아도 손해가 없었다.
  예고·경직·판정은 정상이라 겉으로는 전투가 도는 것처럼 보였다. W4 에서 배선됐다.
- **TC-CMB-08** — `AttackState.startedGrounded` 가 진입 시점에 한 번 찍는 래치인데
  `AllowsHorizontalControl` 이 그것을 매 프레임 "지금 공중인가"로 읽는다.
  **고치지 않은 근거는 체감이 아니라 범위다** — `!IsGrounded` 로 바꾸면 지상 공격 중
  몸이 뜨는 경우까지 같이 정하게 되는데 그 재료가 1차에 없다.

## 3. 무기 스왑 (SWAP)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-SWAP-01 | 외형과 모션이 함께 바뀐다 | 스프라이트·애니메이션이 동시에 교체된다 | W2 신규 | ✔ |
| TC-SWAP-02 | 스왑 대시의 무적 | 대시 중 적 공격에 체력이 안 깎인다 | W2 신규 | ✔ |
| TC-SWAP-03 | 쿨타임 중 스왑 차단 | 게이지가 안 찼으면 입력이 무시된다 | W2 신규 | ✔ |
| TC-SWAP-04 | 스왑 첫 프레임 | 교체 순간 옛 무기 그림이 한 프레임도 안 보인다 | W2 신규 (#46) | ✔ |
| TC-SWAP-05 | 무기 이름 표시 | 로컬라이즈 키가 아니라 `검` 처럼 읽힌다 | W2 #45 직후 빌드 | ✔ |
| TC-SWAP-06 | 스왑 중 조작 차단 | 대시 중 이동·점프·공격·재스왑이 전부 안 먹는다 | 08장 권한표 | △ |

**TC-SWAP-06 은 빌드 문서에 없다.** 포트폴리오 08장이 *"스왑은 다섯을 전부 끈다"* 를
주장하는데 그것을 확인한 기록이 없다. **주장과 검증의 구멍이라 후보로 남긴다.**

## 4. 무기 · 투사체 (WPN)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-WPN-01 | 무기 3종이 다르게 느껴진다 | 검·창·활의 사거리와 리듬이 구분된다 | W2·W3 회귀 | ✔ |
| TC-WPN-02 | 화살이 맞고 사라진다 | 명중하면 소멸하고 발사자는 안 때린다 | W2 신규 | ✔ |
| TC-WPN-03 | 화면 밖 화살이 안 쌓인다 | 몇 분 돌려도 프레임이 안 떨어진다 | W2 계획 5번 | ✔ |
| TC-WPN-04 | 창이 빌드에 들어있다 | 세 무기를 런 안에서 전부 쓸 수 있다 | W3 회귀 | ✔ |
| TC-WPN-05 | 강화가 적용된다 | 강화 후 같은 적을 더 적은 타수로 잡는다 | 최종 결과 1 | ✔ |

**TC-WPN-04 는 W2 에서 실패했던 항목이다.** `Weapon_Bow` 를 씬·코드 어디서도 참조하지
않아 빌드에서 통째로 빠질 뻔했다. `ScriptableObject` 는 누가 참조해야 빌드에 포함된다.
W2 는 검+활로 맞바꿔 냈고 창은 W3 에서 들어왔다.

## 5. 적 · 보스 (ENM)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-ENM-01 | 추격 → 예고 → 공격 → 경직 | 네 단계가 순서대로 보인다 | W2 회귀 | ✔ |
| TC-ENM-02 | 예고 색이 빌드에서 읽힌다 | 공격 직전 색 변화가 화면에 나온다 | W3 신규 | ✔ |
| TC-ENM-03 | 피격이 적 행동을 끊는다 | 때리면 예고·공격이 중단된다 (#79) | W3 신규 | ✔ |
| TC-ENM-04 | 시체가 안 때린다 | 죽은 적의 공격 판정이 남지 않는다 (#55) | W3 신규 | ✔ |
| TC-ENM-05 | 거리로 패턴이 갈린다 | 붙으면 물기, 떨어지면 돌진이 나온다 | W3 신규 | ✔ |
| TC-ENM-06 | 적 역할이 외형으로 갈린다 | 붙기 전에 돌진형·원거리형을 구분할 수 있다 | W4 결과 1 (#96) | ✘ |
| TC-ENM-07 | 원거리 적이 물러선다 | 플레이어가 붙으면 거리를 벌린다 (#108) | 최종 빌드 | ✔ |
| TC-ENM-08 | 보스 3패턴이 전부 나온다 | 한 보스전에서 셋을 다 본다 | 최종 완주 | ✔ |
| TC-ENM-09 | 보스 패턴은 안 끊긴다 | 때려도 보스 공격이 중단되지 않는다 | 패턴 SO `interruptible` | △ |

- **TC-ENM-04** — 에디터에서 손으로 재현하지 못해 미확정으로 남았던 항목이다.
  W3 빌드에서 **편하게 플레이하는 동안 자연히 지나갔다.** 재현이 어려운 조건은
  정조준하는 대신 플레이 시간을 늘리는 쪽이 빠르다.
- **TC-ENM-06** — 코드는 정상이고 화면이 말을 안 한 경우다. W4 에서 처음으로
  *"동작하는가"* 가 아니라 *"알 수 있는가"* 가 걸린 항목이고, #96 에서 수정됐다.

## 6. 방 · 스테이지 (ROOM)

| ID | 확인 항목 | 선행 조건 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|---|
| TC-ROOM-01 | 방이 연달아 전환된다 | | 출구 포탈로 다음 방에 들어간다 | W3 계획 | ✔ |
| TC-ROOM-02 | 방 타입이 핸들러로 갈린다 | | 전투·보상·보스방이 각각 다르게 동작한다 | W3 계획 | ✔ |
| TC-ROOM-03 | 카메라가 방을 따라온다 | | 전환 후 새 방이 화면에 잡힌다 | W4 완주 | ✔ |
| TC-ROOM-04 | 한 런에 방이 겹치지 않는다 | 시드 0~99 | 전투방 5개가 서로 다르다 | 최종 노트 | ✘ → ⚙ |
| TC-ROOM-05 | 같은 시드는 같은 런 | 같은 시드로 2회 | 방 순서가 동일하다 | `RunState` | ⚙ |
| TC-ROOM-06 | 타일 PPU | | 타일이 캐릭터와 같은 해상도로 보인다 (PPU 32) | W3 계획 | ✔ |
| TC-ROOM-07 | 덱 경계에서 연속되지 않는다 | 시드 0~99 · `roomCount` 7 | 5번째와 6번째 방이 다르다 | #141 | ⚙ |
| TC-ROOM-08 | 시드가 다르면 순서도 다르다 | 시드 0~99 | 서로 다른 순서가 둘 이상 나온다 | #141 | ⚙ |

**TC-ROOM-04 가 최종 빌드에서 잡힌 셋 중 하나다.** 매번 무작위로 뽑고 직전 방만
피하는 방식이라 `A B A C B` 가 정상이었다. 시드 10만 개로 재보니 다섯 방이 전부 다른
런은 9.4% 뿐이었다. 덱처럼 섞어 꺼내는 방식으로 바꿔 수정했다.

## 7. 런 루프 · 보상 (RUN)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-RUN-01 | 10칸 완주 | 시작 → 전투 → 보상 → 보스 → 결과가 끊기지 않는다 | 최종 완주 | ✔ |
| TC-RUN-02 | 보스 처치 = 클리어 | 결과 화면이 클리어로 뜬다 | W4 완주 | ✔ |
| TC-RUN-03 | 사망 = 런 종료 | 죽으면 결과 화면이 뜬다 | W4 완주 | ✔ |
| TC-RUN-04 | R 재시작 | 새 시드로 처음부터 다시 시작한다 | W4 결과 | ✔ |
| TC-RUN-05 | 재시작에 이전 런이 안 남는다 | 체력·무기·강화·킬 수가 전부 초기화된다 | W4 결과 | ✔ |
| TC-RUN-06 | 보상 3택 1 | 무기·회복·강화 중 하나를 고른다 | 최종 빌드 (#102) | ✔ |
| TC-RUN-07 | 가진 무기는 후보에서 빠진다 | 이미 든 무기가 카드로 안 나온다 | 최종 결과 3 | ✔ |
| TC-RUN-08 | 보상방 진입이 화면에 드러난다 | 들어간 것을 표시 없이 지나치지 않는다 | W4 결과 2 | ✘ |
| TC-RUN-09 | 처치당 회복 | 적을 잡으면 체력이 조금 오른다 | 최종 빌드 (#136) | ✔ |

- **TC-RUN-05 가 W4 의 진짜 관문이었다.** 빌드에는 도메인 리로드가 없어 `static` 이
  살아남는다. 런 상태를 `static` 이 아니라 `RunState` 인스턴스로 모아둔 덕에
  재시작이 새 인스턴스를 만드는 것만으로 초기화됐다.
- **TC-RUN-08** — W4 에서는 보상방에 들어가면 조용히 10 회복되고 끝났다. 방 생김새도
  전투방과 같아서 거기가 보상방인지 알 방법이 없었다. #102 의 완료 조건에
  *"진입이 화면으로 드러난다"* 를 넣어 해결했다.

## 8. UI · 설정 · 로컬라이즈 (UI)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-UI-01 | 시작 화면 첫 프레임 | 켜자마자 글자가 깜빡이지 않는다 | 최종 노트 | ✘ |
| TC-UI-02 | 시작 화면 조작 | 게임 시작 · 조작 안내 · 종료가 동작한다 | 최종 확인 범위 | ✔ |
| TC-UI-03 | ESC 일시정지 | 전투 중 ESC 로 멈추고 설정이 열린다 | 최종 확인 범위 | ✔ |
| TC-UI-04 | 설정 5항목 | 해상도·화면 모드·볼륨·언어가 즉시 반영된다 | 최종 확인 범위 | ✔ |
| TC-UI-05 | 설정 저장 | 껐다 켜도 설정이 유지된다 | #140 | △ |
| TC-UI-06 | 전투 중 언어 항목 | 전투 씬 설정에는 언어가 안 보인다 | 커밋 5578ebc | ✔ |
| TC-UI-07 | 영어 안내 문단 | 영어로 바꿔도 글자가 상자를 안 넘는다 | 최종 노트 | ✘ |
| TC-UI-08 | HUD | 체력 바·무기 슬롯·스왑 게이지가 상태를 따라간다 | 최종 빌드 (#112) | ✔ |
| TC-UI-09 | 결과 화면 로컬라이즈 | 영어에서 결과 화면이 한국어로 안 나온다 | W4 확인 안 한 것 (#103) | ✔ |

- **TC-UI-01 · TC-UI-07 이 최종 빌드에서 잡힌 나머지 둘이다.** 둘 다 에디터에서는
  증상이 없다. 영어 문장이 한국어보다 1.4~1.9배 길어 줄 수가 늘면서 상자를 넘었고,
  **빌드를 영어로 실행해 보고서야 발견했다.**
- **TC-UI-05 는 확인 기록이 없다.** 설정 저장은 #140 에 들어갔지만 재실행 후 유지를
  확인한 문장이 빌드 결과에 없다. 한 번 눌러보면 끝나는 항목이라 후보로 남긴다.

## 9. 소리 (SND)

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-SND-01 | 효과음 5종 | 타격·피격·사망·스왑·보상이 각각 난다 | 최종 확인 범위 | ✔ |
| TC-SND-02 | BGM | 시작 화면과 전투에서 재생된다 | 최종 확인 범위 | ✔ |
| TC-SND-03 | 볼륨 반영 | 설정에서 내리면 즉시 작아진다 | #139·#140 | △ |

## 10. 빌드 전 정적 확인 (BLD)

**이 묶음만 성격이 다르다.** 플레이가 아니라 파일과 설정을 보는 것이고,
[W4 계획](builds/4주차%20다음%20빌드%20계획.md)의 목록이 그대로 TC 가 된다.
*"빌드는 확인하는 자리가 아니라 확인한 것을 증명하는 자리다."*

| ID | 확인 항목 | 기대 결과 | 출처 | 결과 |
|---|---|---|---|---|
| TC-BLD-01 | 컴파일 에러 0 | 콘솔이 비어 있다 | W4·최종 계획 | ✔ |
| TC-BLD-02 | Pixem `using UnityEditor` 가드 | 세 파일 전부 `#if UNITY_EDITOR` 안에 있다 | W1 결과 | ✘ |
| TC-BLD-03 | SO 참조 사슬 | 무기·방·패턴 SO 가 전부 누군가에게 참조된다 | W2 결과 | ✘ |
| TC-BLD-04 | 빌드 씬 목록 | 시작 씬이 index 0 · 씬 2개 | 최종 계획 | ✔ |
| TC-BLD-05 | 테스트 값 잔류 | `roomCount` · `seed` · `maxHealth` · `healAmount` 가 실서비스 값 | 최종 계획 | ✔ |
| TC-BLD-06 | 씬 저장 | `git status` 에 미저장 씬이 없다 | 최종 계획 | ✔ |
| TC-BLD-07 | UI 배선 | HealthBar · 결과 · 보상 · 시작 화면 참조가 안 비었다 | 최종 계획 | ✔ |
| TC-BLD-08 | `bundleVersion` | 이번 빌드 번호와 일치한다 | 최종 계획 | ✔ |
| TC-BLD-09 | 실행 후 `Player.log` | 예외가 없다 | 최종 확인 범위 | ✔ |

- **TC-BLD-02 는 W1 을 깨뜨린 항목이다.** 클래스 본문은 가드 안이라 에디터에서는
  증상이 전혀 없고 플레이어 빌드에서만 `CS0246` 이 난다. **에셋을 다시 임포트하면
  되돌아가므로** 매 빌드 확인 항목으로 남는다.
- **TC-BLD-05 가 반복 실수다.** 보상방까지 가서 확인하려면 `maxHealth` 를 올릴 일이
  생기고, 되돌리는 것을 잊는다.
- W4 에서 이 목록으로 **빌드 왕복이 0** 이 됐다.

---

## 자동화된 것

`Assets/Tests/RunStateTests.cs` · EditMode · 테스트 4개 ([#141](../../issues/141)).
Test Runner 의 EditMode 탭에서 돌린다.

| 테스트 | 대응 TC |
|---|---|
| 같은 시드는 같은 방 순서다 | TC-ROOM-05 |
| 한 런에 전투방이 겹치지 않는다 | TC-ROOM-04 |
| 덱 경계에서 같은 방이 연속되지 않는다 | TC-ROOM-07 |
| 시드가 다르면 순서도 달라진다 | TC-ROOM-08 |

**넷째는 앞의 셋이 통과해도 셔플을 검증하지 못해서 붙였다.** 셔플을 지워 풀 순서 그대로
나오게 해도 앞의 셋은 전부 통과한다 (학습 로그 5절 "통과하는 테스트와 검증하는 테스트는
다르다").

시드를 0~99 로 도는 것은 표본을 늘리려는 게 아니라 **분기에 닿기 위해서다.** 덱 경계 회피
코드는 새 덱의 첫 방이 직전 방과 같을 때만 실행되고 그 확률이 1/5 다.

## 아직 자동화 안 한 것

플레이로 판정할 수 없어 인게임 TC 에서 뺀 것들이다. 전부 씬 없이 EditMode 에서
돌릴 수 있는 위치에 있다.

| 무엇을 | 어디서 | 왜 인게임에서 못 하나 |
|---|---|---|
| 겹치는 사거리에서 가중치 3:1 | `WeightedPatternSelector` | 확률은 한 판으로 안 보인다 |
| 높이차가 근접 패턴을 끊는다 | `EnemyBrain.TrySelectPattern` | 높이차를 눈으로 못 잰다 |
| 쿨다운 중 패턴 제외 | `WeightedPatternSelector` | 초 단위를 못 잰다 |
| 강화 1레벨에 최소 +1 | `WeaponInstance.GetAttack` | 타수로 역산해야 한다 |
| `GetAttack` 이 원본 SO 를 안 바꾼다 | `WeaponInstance` | 값 복사 여부가 화면에 안 나온다 |
| 코요테 0.1 · 버퍼 0.15 · 대시 0.2 · 무적 0.3 | 각 컴포넌트 | 프레임 단위 시간 |

**여기부터는 SO 픽스처가 먼저다.** `AttackPattern` · `WeaponData` 는 필드가 전부
`[SerializeField] private` 이라 테스트에서 값을 넣을 수 없다. `SerializedObject` 주입이나
실제 에셋 로드 중 하나를 정해야 한다 — 테스트 때문에 `public` 으로 여는 것은 순서가 거꾸로다.

패턴 선택기는 **판단이 하나 더 필요하다.** 높이 조건이 선택기가 아니라
`EnemyBrain.TrySelectPattern` 에 있고, 그 전에 `Select()` 가 쿨다운을 이미 소모한다.
거절된 패턴이 한 번도 안 나가고 쿨다운만 도는 것이 의도인지에 따라 테스트 문장이 갈린다.

---

## 남은 구멍

이 목록을 만들면서 **검증 기록이 없는 자리**가 드러났다.

| | 왜 비어 있나 |
|---|---|
| TC-SWAP-06 스왑 중 조작 차단 | 08장이 주장하는데 확인한 기록이 없다 |
| ~~TC-ROOM-05 같은 시드는 같은 런~~ | **자동 테스트로 채웠다** ([#141](../../issues/141)) |
| TC-UI-05 설정 저장 | 재실행 후 유지를 확인한 문장이 없다 |
| TC-SND-03 볼륨 반영 | 소리가 나는 것만 확인했다 |
| TC-ENM-09 보스 패턴 비중단 | 데이터는 `interruptible: 0` 인데 플레이 확인 기록이 없다 |
| 게임패드 · 다른 해상도 · 다른 PC · 장시간 | 매 빌드 "확인 안 한 것"에 그대로 남아 있다 |

**마지막 줄이 다섯 번 연속으로 같은 자리에 있다.** 확인 안 한 것을 매번 적어둔 덕에
누적됐다는 것이 보인다.
